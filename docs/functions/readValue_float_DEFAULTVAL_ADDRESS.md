# readValue_float_DEFAULTVAL_ADDRESS @ 0x3E1AA

**Purpose:** Read a 32-bit IEEE-754 single-precision float from RAM at a given address, with validation and fallback to a default float value if the data is corrupted.
In: `r4` (r14): address in RAM (raw pointer, typically 0xFFFF...) ; `fr4`: default/fallback float value  Out: `fr0`: the read float value, or the default if validation fails ; Modifies status register (getSR/setSR pair disables/restores interrupts)  Calls: `getSR` (0x3920): save and disable interrupts ; `setMemInsideFUNCto1` (0x3E3F0): mark as invalid if data is corrupted ; `setSR` (0x3934): restore interrupts  Behavior: Save r14, fr15 to stack (float is caller-saved) ; Copy input address (r4) → r14 ; Copy default float from fr4 → fr15 (preserved across calls) ; Disable interrupts with `getSR(16)` ; Read word (16-bit) at [r14] → r3 ; Read word (16-bit) at [r14+2] → r0 ; Add r3 + r0 → r0 (sum of two halves) ; Calculate checksum complement: ~r0 → r4, zero-extend to 16-bit ; Read word (16-bit) at [r14+4] (first validation word) → r0 ; Compare validation: if r0 == checksum complement, continue... ; Read word (16-bit) at [r14+6] (second validation word) → r0 ; Compare validation: if r0 == checksum complement, data is valid ; If valid, read the float at [r14] → fr15 ; If invalid, call `setMemInsideFUNCto1` and use default (fr15 already set to default) ; Restore interrupts with `setSR(16)` ; Move fr15 → fr0 (return value) ; Clean up stack and return
**Status:** med (float handling is clear; dual validation check is unusual but pattern fits redundancy model; exact checksum algorithm inferred from word summation) ; Purpose of dual validation words (offset +4, +6) unclear—may be two independent checksums or a split structure ; Whether the checksum is computed as simple sum of two halves or if additional processing exists ; Stack frame offsets and float argument transfer are architecture-standard but not fully verified ; Whether addr is always a 0xFFFF... pointer or can be an index/handle
