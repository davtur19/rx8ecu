# setEngineRunningInjectorsOffFlag @ 0xE2AC

**Purpose:** Set a flag that tells whether injectors must shut off while the engine is still running (safety cutoff during coast-down or a fault condition).
In: @0xA42C: injectors_off_flag (output address) ; @0xA428: engine_running flag (1=running, 0=off) ; @0xA41C: some enable/disable or fault flag  Out: Writes 1 or 0 to @0xA42C (injectors_off flag) ; Condition: set to 1 if (engine_running==1 AND flag@0xA41C==0), else 0  Calls: None (inline logic only).  Behavior: Load the target address 0xA42C into r4 ; Load the engine_running flag from 0xA428 into r3 ; Test r3 (if zero, the engine is off) ; If the engine is NOT running: jump to step 7 (set flag to 0) ; Load the flag from 0xA41C into r0 ; Test r0 ; If that flag is non-zero: jump to step 9 (set to 0) ; Set r2=1, branch to step 10 ; Set r1=0 ; Write r2 or r1 to @r4 (0xA42C)
**Status:** med — control flow is clear, but the semantic meaning of the @0xA41C flag is unknown. The name suggests injector cutoff logic. ; What condition at 0xA41C triggers the injector cutoff? (fault? throttle closed? over-rev?) ; Does injectors_off=1 mean "shut them off now" or "safe to leave off"? ; When does this flag get checked or acted upon?
