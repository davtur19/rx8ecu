#!/usr/bin/env python3
"""Differential test for fuel_trim_decay_controller (0x19E98) — v3 lift (branches + delay slots), 90 bytes.
Auto-generated by tools/gen_c_lift_v3.py — not human-verified.
Compares a Python pc-interpreter spec_mirror against the sh2emu oracle
(which runs the actual ROM bytes) over 500 random inputs: deterministic RAM
prefill around the literal addresses plus a synthetic 0x400-byte stack at
STACK_BASE.  Branch cond is sampled on T BEFORE the delay slot (as sh2emu);
cases where either side leaves the modeled span / exceeds max_steps are
skipped (StepLimitExceeded -> skip).
Run from repo root: python3 c/tests/test_fuel_trim_decay_controller_19e98.py
"""
import os, random, sys

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, os.path.join(ROOT, "tools"))
from sh2emu import SH2, StepLimitExceeded
from c_lift_ops import s8, s16, s32

ROM = os.path.join(ROOT, "roms", "stock", "60E1D400.bin")
ROM_BYTES = open(ROM, "rb").read()
ENTRY = 0x19E98
RAW = bytes.fromhex("D4 17 93 2A 60 30 60 0C 88 01 8F 04 00 09 D2 15 61 21 A0 0F 24 11 92 21 60 20 60 0C 88 01 8F 09 00 09 60 41 60 0D 40 15 8F 04 00 09 D1 0E 60 41 30 1C 24 01 95 13 63 41 63 3D 43 15 8D 06 00 09 D3 0A 60 30 60 0C 88 01 8F 03 00 09 E3 01 A0 02 25 30 E1 00 25 10 00 0B 00 09")
SEED = 42
N = 500
MAXSTEPS = 100000
STACK_BASE = 0xFFFFD000
STACK_TOP = STACK_BASE + 0x400
STACK_OFFS = ()
RAM_MIN = 0xFFFFA9E9
RAM_MAX = 0xFFFFAAE0
SPAN_END = 0x19EF2
SENT = 0x19F08     # stop-sentinel, placed past the literal pool

_WRITES = []

def _rd(ram, a):
    a &= 0xFFFFFFFF
    v = ram.get(a)
    if v is not None:
        return v
    return ROM_BYTES[a] if a < len(ROM_BYTES) else 0

def _rdw(ram, a, n):
    a &= 0xFFFFFFFF
    return int.from_bytes(bytes(_rd(ram, (a + i) & 0xFFFFFFFF) for i in range(n)), "big")

def _wrw(ram, a, n, v):
    a &= 0xFFFFFFFF
    for i in range(n):
        ad = (a + i) & 0xFFFFFFFF
        ram[ad] = (v >> (8 * (n - 1 - i))) & 0xFF
        _WRITES.append(ad)

CODE = {
    0x19e98: {"kind": 'reg', "py": 'r[4] = 0xFFFFA9EC', "slot_py": None, "target": None, "cond": None},
    0x19e9a: {"kind": 'reg', "py": 'r[3] = 0xFFFFA9EA', "slot_py": None, "target": None, "cond": None},
    0x19e9c: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFA9EA, 1))', "slot_py": None, "target": None, "cond": None},
    0x19e9e: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x19ea0: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x01) else 0', "slot_py": None, "target": None, "cond": None},
    0x19ea2: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19eae, "cond": 'notT'},
    0x19ea6: {"kind": 'reg', "py": 'r[2] = 0x0006F89A', "slot_py": None, "target": None, "cond": None},
    0x19ea8: {"kind": 'mem', "py": 'r[1] = s16(_rdw(ram, 0x0006F89A, 2))', "slot_py": None, "target": None, "cond": None},
    0x19eaa: {"kind": "branch", "py": None, "slot_py": '_wrw(ram, 0xFFFFA9EC, 2, r[1])', "target": 0x19ecc, "cond": 'always'},
    0x19eae: {"kind": 'reg', "py": 'r[2] = 0xFFFFAAE0', "slot_py": None, "target": None, "cond": None},
    0x19eb0: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFAAE0, 1))', "slot_py": None, "target": None, "cond": None},
    0x19eb2: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x19eb4: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x01) else 0', "slot_py": None, "target": None, "cond": None},
    0x19eb6: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19ecc, "cond": 'notT'},
    0x19eba: {"kind": 'mem', "py": 'r[0] = s16(_rdw(ram, 0xFFFFA9EC, 2))', "slot_py": None, "target": None, "cond": None},
    0x19ebc: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFFFF', "slot_py": None, "target": None, "cond": None},
    0x19ebe: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) > 0 else 0', "slot_py": None, "target": None, "cond": None},
    0x19ec0: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19ecc, "cond": 'notT'},
    0x19ec4: {"kind": 'reg', "py": 'r[1] = 0x0000FFFF', "slot_py": None, "target": None, "cond": None},
    0x19ec6: {"kind": 'mem', "py": 'r[0] = s16(_rdw(ram, 0xFFFFA9EC, 2))', "slot_py": None, "target": None, "cond": None},
    0x19ec8: {"kind": 'reg', "py": 'r[0] = (r[0] + r[1]) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0x19eca: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFA9EC, 2, r[0])', "slot_py": None, "target": None, "cond": None},
    0x19ecc: {"kind": 'reg', "py": 'r[5] = 0xFFFFA9E9', "slot_py": None, "target": None, "cond": None},
    0x19ece: {"kind": 'mem', "py": 'r[3] = s16(_rdw(ram, 0xFFFFA9EC, 2))', "slot_py": None, "target": None, "cond": None},
    0x19ed0: {"kind": 'reg', "py": 'r[3] = r[3] & 0xFFFF', "slot_py": None, "target": None, "cond": None},
    0x19ed2: {"kind": 'reg', "py": 'T = 1 if s32(r[3]) > 0 else 0', "slot_py": None, "target": None, "cond": None},
    0x19ed4: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19ee4, "cond": 'T'},
    0x19ed8: {"kind": 'reg', "py": 'r[3] = 0xFFFFA9F0', "slot_py": None, "target": None, "cond": None},
    0x19eda: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFA9F0, 1))', "slot_py": None, "target": None, "cond": None},
    0x19edc: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x19ede: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x01) else 0', "slot_py": None, "target": None, "cond": None},
    0x19ee0: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19eea, "cond": 'notT'},
    0x19ee4: {"kind": 'reg', "py": 'r[3] = s8(0x01)', "slot_py": None, "target": None, "cond": None},
    0x19ee6: {"kind": "branch", "py": None, "slot_py": '_wrw(ram, 0xFFFFA9E9, 1, r[3])', "target": 0x19eee, "cond": 'always'},
    0x19eea: {"kind": 'reg', "py": 'r[1] = s8(0x00)', "slot_py": None, "target": None, "cond": None},
    0x19eec: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFA9E9, 1, r[1])', "slot_py": None, "target": None, "cond": None},
    0x19eee: {"kind": "ret", "py": None, "slot_py": '', "target": None, "cond": None},}

def spec_mirror(r4, r5, r6, r7, ram):
    """pc-interpreter over CODE; returns ("RET", regs, writes, ram, pr) or
    ("SKIP"/"ERR", detail).  Every instruction is the mapper py fragment
    exec'd in a shared ns (registers/T/ram/writes follow sh2emu)."""
    global _WRITES
    _WRITES[:] = []
    r = [0] * 16
    r[4], r[5], r[6], r[7] = r4 & 0xFFFFFFFF, r5 & 0xFFFFFFFF, r6 & 0xFFFFFFFF, r7 & 0xFFFFFFFF
    r[15] = STACK_TOP & 0xFFFFFFFF
    ns = {"r": r, "T": 0, "Q": 0, "M": 0, "mach": 0, "macl": 0, "pr": 0xEEEE0000,
          "s8": s8, "s16": s16, "s32": s32, "ram": ram, "sp": r[15],
          "local": {off: _rdw(ram, STACK_BASE + off, 4) for off in STACK_OFFS},
          "_rdw": _rdw, "_wrw": _wrw, "STACK_BASE": STACK_BASE}
    pc = ENTRY
    steps = 0
    while True:
        steps += 1
        if steps > MAXSTEPS:
            return ("SKIP", None)
        inst = CODE.get(pc)
        if inst is None:
            return ("ERR", pc)
        kind = inst["kind"]
        if kind == "branch":
            t = ns["T"]
            cond = inst["cond"]
            taken = True
            if cond == "T":
                taken = (t == 1)
            elif cond == "notT":
                taken = (t == 0)
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            if taken:
                pc = inst["target"]
            elif slot_py is not None:
                pc = pc + 4            # delayed: slot at P+2 already ran
            else:
                pc = pc + 2            # non-delayed bt/bf
        elif kind == "ret":
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            r[15] = ns["sp"] & 0xFFFFFFFF
            return ("RET", [x & 0xFFFFFFFF for x in r], _WRITES, ram, ns["pr"] & 0xFFFFFFFF)
        else:
            py = inst["py"]
            if py:
                exec(py, ns)
            pc = pc + 2

def run(cpu, ram, a, b, c_, d):
    ram = dict(ram)
    ram[SENT] = 0x00; ram[SENT + 1] = 0x0B; ram[SENT + 2] = 0x00; ram[SENT + 3] = 0x09
    cpu.call(ENTRY, r4=a, r5=b, r6=c_, r7=d, ram=ram, regs={15: STACK_TOP},
             max_steps=MAXSTEPS)
    out = dict(cpu.ram)
    for i in range(4):
        out.pop(SENT + i, None)
    return cpu.r[0] & 0xFFFFFFFF, [x & 0xFFFFFFFF for x in cpu.r], out, cpu.pr & 0xFFFFFFFF

def main():
    rnd = random.Random(SEED)
    cpu = SH2(ROM_BYTES)
    skipped = 0
    for caso in range(N):
        ram = {}
        if RAM_MIN is not None:
            for a in range(RAM_MIN - 0x400, RAM_MAX + 0x401):
                ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        for a in range(STACK_BASE, STACK_BASE + 0x400):
            ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        a = rnd.randint(0, 0xFFFFFFFF)
        b = rnd.randint(0, 0xFFFFFFFF)
        c_ = rnd.randint(0, 0xFFFFFFFF)
        d = rnd.randint(0, 0xFFFFFFFF)
        m = spec_mirror(a, b, c_, d, dict(ram))
        if m[0] != "RET":
            skipped += 1
            continue
        try:
            g = run(cpu, ram, a, b, c_, d)
        except (StepLimitExceeded, NotImplementedError, RuntimeError):
            skipped += 1
            continue
        _, exp_regs, _, exp_ram, exp_pr = m
        _, got_regs, got_ram, got_pr = g
        for i in range(16):
            if exp_regs[i] != got_regs[i]:
                print("MISMATCH case=%d reg=r%d mirror=%08X emu=%08X" % (caso, i, exp_regs[i], got_regs[i]))
                sys.exit(1)
        if exp_pr != got_pr:
            print("MISMATCH case=%d reg=pr mirror=%08X emu=%08X" % (caso, exp_pr, got_pr))
            sys.exit(1)
        for ad in sorted(set(exp_ram) | set(got_ram)):
            if exp_ram.get(ad, 0) != got_ram.get(ad, 0):
                print("MISMATCH case=%d addr=0x%08X mirror=%02X emu=%02X" % (caso, ad, exp_ram.get(ad, 0), got_ram.get(ad, 0)))
                sys.exit(1)
    ok = N - skipped
    if skipped > 200 or ok == 0:
        print("FAIL %d/%d (skipped=%d)" % (ok, N, skipped))
        sys.exit(1)
    print("PASS %d/%d (skipped=%d)" % (ok, N, skipped))

if __name__ == "__main__":
    main()
