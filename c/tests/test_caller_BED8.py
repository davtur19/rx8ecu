#!/usr/bin/env python3
"""Differential test for caller_BED8 (entry 0xBED8) — v8 ST caller (CFG), 98 bytes.
Auto-generated by tools/gen_c_lift_v8.py — not human-verified.
Compares a Python pc-interpreter spec_mirror (running the caller records
with branch labels/goto semantics + the inlined callee leaves; jmp/jsr
jump to their targets, jump tables read the ROM table dynamically) against
the sh2emu oracle (which runs the actual ROM bytes) over 20 random
inputs.  Cases leaving the modeled span / exceeding max_steps are skipped.
Run: python3 test_caller_BED8.py
"""
import os, random, sys

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, os.path.join(ROOT, "tools"))
from sh2emu import SH2, StepLimitExceeded
from c_lift_ops import s8, s16, s32, ts, bits2f, f2bits

ROM = os.path.join(ROOT, "roms", "stock", "60E0FC00.bin")
ROM_BYTES = open(ROM, "rb").read()
ENTRY = 0xBED8
SEED = 42
N = 20
MAXSTEPS = 100000
STACK_BASE = 0xFFFFD000
STACK_TOP = STACK_BASE + 0x400
STACK_OFFS = (0x3E4, 0x3E8, 0x3EC, 0x3F0, 0x3F4, 0x3F8, 0x3FC)
RAM_MIN = 0xFFFFF738
RAM_MAX = 0xFFFFF738
PRET = 0xEEEE0000
JTABLES = []
SLOT_SEEDS = ()

_WRITES = []

def _rd(ram, a):
    a &= 0xFFFFFFFF
    v = ram.get(a)
    if v is not None:
        return v
    return ROM_BYTES[a] if a < len(ROM_BYTES) else 0

def _rdw(ram, a, n):
    a &= 0xFFFFFFFF
    return int.from_bytes(bytes(_rd(ram, (a + i) & 0xFFFFFFFF) for i in range(n)), "big")

def _wrw(ram, a, n, v):
    a &= 0xFFFFFFFF
    for i in range(n):
        ad = (a + i) & 0xFFFFFFFF
        ram[ad] = (v >> (8 * (n - 1 - i))) & 0xFF
        _WRITES.append(ad)

CODE = {
    0xbed8: {"kind": 'mem', "py": '_wrw(ram, (sp - 4) & 0xFFFFFFFF, 4, r[14])\nsp = (sp - 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbeda: {"kind": 'mem', "py": '_wrw(ram, (sp - 4) & 0xFFFFFFFF, 4, r[13])\nsp = (sp - 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbedc: {"kind": 'mem', "py": '_wrw(ram, (sp - 4) & 0xFFFFFFFF, 4, r[12])\nsp = (sp - 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbede: {"kind": 'mem', "py": '_wrw(ram, (sp - 4) & 0xFFFFFFFF, 4, r[11])\nsp = (sp - 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbee0: {"kind": 'st', "py": '_wrw(ram, (sp - 4) & 0xFFFFFFFF, 4, pr)\nsp = (sp - 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbee2: {"kind": 'reg', "py": 'r[15] = (r[15] + s8(0xF8)) & 0xFFFFFFFF\nsp = (sp + s8(0xF8)) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbee4: {"kind": 'reg', "py": 'r[5] = 0x000000E0', "slot_py": None, "target": None, "cond": None},
    0xbee6: {"kind": 'reg', "py": 'r[4] = sp', "slot_py": None, "target": None, "cond": None},
    0xbee8: {"kind": 'reg', "py": 'r[3] = 0x00002054', "slot_py": None, "target": None, "cond": None},
    0xbeea: {"kind": "call", "py": None, "slot_py": 'r[4] = (r[4] + s8(0x04)) & 0xFFFFFFFF', "target": 0x2054, "ret_pc": 0xbeee, "set_pr": True, "cond": None},
    0xbeee: {"kind": 'reg', "py": 'r[6] = s8(0x01)', "slot_py": None, "target": None, "cond": None},
    0xbef0: {"kind": 'reg', "py": 'r[12] = 0xFFFFF74E', "slot_py": None, "target": None, "cond": None},
    0xbef2: {"kind": 'reg', "py": 'r[13] = 0x00000400', "slot_py": None, "target": None, "cond": None},
    0xbef4: {"kind": 'reg', "py": 'r[14] = 0x00004BBC', "slot_py": None, "target": None, "cond": None},
    0xbef6: {"kind": 'reg', "py": 'r[5] = r[13]', "slot_py": None, "target": None, "cond": None},
    0xbef8: {"kind": "call", "py": None, "slot_py": 'r[4] = r[12]', "target": 0x4bbc, "ret_pc": 0xbefc, "set_pr": True, "cond": None},
    0xbefc: {"kind": 'reg', "py": 'r[3] = 0x00002064', "slot_py": None, "target": None, "cond": None},
    0xbefe: {"kind": "call", "py": None, "slot_py": 'r[4] = _rdw(ram, sp + 4, 4)', "target": 0x2064, "ret_pc": 0xbf02, "set_pr": True, "cond": None},
    0xbf02: {"kind": 'reg', "py": 'r[2] = 0x0000239C', "slot_py": None, "target": None, "cond": None},
    0xbf04: {"kind": "call", "py": None, "slot_py": 'r[4] = s8(0x01)', "target": 0x239c, "ret_pc": 0xbf08, "set_pr": True, "cond": None},
    0xbf08: {"kind": 'reg', "py": 'r[2] = 0xFFFFF738', "slot_py": None, "target": None, "cond": None},
    0xbf0a: {"kind": 'mem', "py": 'r[3] = s16(_rdw(ram, r[2], 2))', "slot_py": None, "target": None, "cond": None},
    0xbf0c: {"kind": 'reg', "py": 'r[3] = r[3] & 0xFFFF', "slot_py": None, "target": None, "cond": None},
    0xbf0e: {"kind": 'reg', "py": 'r[1] = 0x00000800', "slot_py": None, "target": None, "cond": None},
    0xbf10: {"kind": 'reg', "py": 'r[5] = 0x000000E0', "slot_py": None, "target": None, "cond": None},
    0xbf12: {"kind": 'reg', "py": 'T = 1 if (r[3] & r[1]) == 0 else 0', "slot_py": None, "target": None, "cond": None},
    0xbf14: {"kind": 'reg', "py": 'r[2] = 0x00002054', "slot_py": None, "target": None, "cond": None},
    0xbf16: {"kind": 'reg', "py": 'r[11] = T;', "slot_py": None, "target": None, "cond": None},
    0xbf18: {"kind": "call", "py": None, "slot_py": 'r[4] = sp', "target": 0x2054, "ret_pc": 0xbf1c, "set_pr": True, "cond": None},
    0xbf1c: {"kind": 'reg', "py": 'r[6] = s8(0x00)', "slot_py": None, "target": None, "cond": None},
    0xbf1e: {"kind": 'reg', "py": 'r[5] = r[13]', "slot_py": None, "target": None, "cond": None},
    0xbf20: {"kind": "call", "py": None, "slot_py": 'r[4] = r[12]', "target": 0x4bbc, "ret_pc": 0xbf24, "set_pr": True, "cond": None},
    0xbf24: {"kind": 'reg', "py": 'r[3] = 0x00002064', "slot_py": None, "target": None, "cond": None},
    0xbf26: {"kind": "call", "py": None, "slot_py": 'r[4] = _rdw(ram, sp, 4)', "target": 0x2064, "ret_pc": 0xbf2a, "set_pr": True, "cond": None},
    0xbf2a: {"kind": 'reg', "py": 'r[0] = r[11]', "slot_py": None, "target": None, "cond": None},
    0xbf2c: {"kind": 'reg', "py": 'r[15] = (r[15] + s8(0x08)) & 0xFFFFFFFF\nsp = (sp + s8(0x08)) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbf2e: {"kind": 'st', "py": 'pr = _rdw(ram, sp, 4)\nsp = (sp + 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbf30: {"kind": 'mem', "py": 'r[11] = _rdw(ram, sp, 4)\nsp = (sp + 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbf32: {"kind": 'mem', "py": 'r[12] = _rdw(ram, sp, 4)\nsp = (sp + 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbf34: {"kind": 'mem', "py": 'r[13] = _rdw(ram, sp, 4)\nsp = (sp + 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xbf36: {"kind": "ret", "py": None, "slot_py": 'r[14] = _rdw(ram, sp, 4)\nsp = (sp + 4) & 0xFFFFFFFF', "target": None, "cond": None, },
    0x2054: {"kind": 'reg', "py": 'r[0] = sr', "slot_py": None, "target": None, "cond": None},
    0x2056: {"kind": 'reg', "py": 'r[0] = (r[0] & 0xF0) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0x2058: {"kind": 'reg', "py": 'T = 1 if (r[5] & 0xFFFFFFFF) >= (r[0] & 0xFFFFFFFF) else 0', "slot_py": None, "target": None, "cond": None},
    0x205a: {"kind": "branch", "py": None, "slot_py": '_wrw(ram, r[4], 4, r[0])', "target": 0x2060, "cond": 'T'},
    0x205e: {"kind": 'reg', "py": 'r[5] = r[0]', "slot_py": None, "target": None, "cond": None},
    0x2060: {"kind": "ret", "py": None, "slot_py": 'sr = r[5]', "target": None, "cond": None, },
    0x2064: {"kind": "ret", "py": None, "slot_py": 'sr = r[4]', "target": None, "cond": None, },
    0x239c: {"kind": 'reg', "py": 'r[5] = s8(0x00)', "slot_py": None, "target": None, "cond": None},
    0x239e: {"kind": 'reg', "py": 'r[4] = (r[4] << 2) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0x23a0: {"kind": 'reg', "py": 'T = (r[4] >> 31) & 1\nr[4] = (r[4] << 1) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0x23a2: {"kind": 'reg', "py": 'T = 1 if (r[5] & 0xFFFFFFFF) >= (r[4] & 0xFFFFFFFF) else 0', "slot_py": None, "target": None, "cond": None},
    0x23a4: {"kind": "branch", "py": None, "slot_py": None, "target": 0x23ac, "cond": 'T'},
    0x23a6: {"kind": 'reg', "py": 'r[5] = (r[5] + s8(0x01)) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0x23a8: {"kind": 'reg', "py": 'T = 1 if (r[5] & 0xFFFFFFFF) >= (r[4] & 0xFFFFFFFF) else 0', "slot_py": None, "target": None, "cond": None},
    0x23aa: {"kind": "branch", "py": None, "slot_py": None, "target": 0x23a6, "cond": 'notT'},
    0x23ac: {"kind": "ret", "py": None, "slot_py": '', "target": None, "cond": None, },
    0x4bbc: {"kind": 'mem', "py": 'r[3] = s16(_rdw(ram, r[4], 2))', "slot_py": None, "target": None, "cond": None},
    0x4bbe: {"kind": 'reg', "py": 'r[6] = r[6] & 0xFFFF', "slot_py": None, "target": None, "cond": None},
    0x4bc0: {"kind": 'reg', "py": 'T = 1 if (r[6] & r[6]) == 0 else 0', "slot_py": None, "target": None, "cond": None},
    0x4bc2: {"kind": "branch", "py": None, "slot_py": None, "target": 0x4bcc, "cond": 'notT'},
    0x4bc4: {"kind": 'reg', "py": 'r[5] = (~r[5]) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0x4bc6: {"kind": 'reg', "py": 'r[3] = (r[3] & r[5]) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0x4bc8: {"kind": "ret", "py": None, "slot_py": '_wrw(ram, r[4], 2, r[3])', "target": None, "cond": None, },
    0x4bcc: {"kind": 'reg', "py": 'r[3] = (r[3] | r[5]) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0x4bce: {"kind": 'mem', "py": '_wrw(ram, r[4], 2, r[3])', "slot_py": None, "target": None, "cond": None},
    0x4bd0: {"kind": "ret", "py": None, "slot_py": '', "target": None, "cond": None, },
}
def spec_mirror(r4, r5, r6, r7, ram):
    global _WRITES
    _WRITES[:] = []
    r = [0] * 16
    r[4], r[5], r[6], r[7] = r4 & 0xFFFFFFFF, r5 & 0xFFFFFFFF, r6 & 0xFFFFFFFF, r7 & 0xFFFFFFFF
    r[15] = STACK_TOP & 0xFFFFFFFF
    fr = [0.0] * 16
    ns = {"r": r, "fr": fr, "T": 0, "Q": 0, "M": 0, "mach": 0, "macl": 0, "pr": PRET,
          "sr": 0x000000F0, "gbr": 0, "s8": s8, "s16": s16, "s32": s32, "ts": ts,
          "bits2f": bits2f, "f2bits": f2bits, "ram": ram,
          "sp": r[15], "_rdw": _rdw, "_wrw": _wrw, "STACK_BASE": STACK_BASE,
          "local": {off: _rdw(ram, STACK_BASE + off, 4) for off in STACK_OFFS}}
    pc = ENTRY
    steps = 0
    while True:
        # (v8.8) keep r[15] in lockstep with the runtime `sp` alias.  The
        # stack opcodes (sts.l pr,@-r15 / add #imm,r15 / mov.l @r15+,Rn)
        # are emitted sp-relative (sp updates only); any instruction that
        # COPIES r15 into another register (frame ptr / wrapper arg, e.g.
        # mov r15,r4) would otherwise read a stale r[15] one push ahead of
        # sp, making the callee store/load the WRONG stack slot (MISMATCH
        # case=0 reg=r0/r2/r3/r4 on caller wrappers).
        r[15] = ns["sp"] & 0xFFFFFFFF
        steps += 1
        if steps > MAXSTEPS:
            return ("SKIP", None)
        inst = CODE.get(pc)
        if inst is None:
            return ("RET", [x & 0xFFFFFFFF for x in r], list(_WRITES), dict(ram),
                    ns["pr"] & 0xFFFFFFFF)
        kind = inst["kind"]
        if kind == "jt":
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            reg = inst["reg"]; base = inst["base"]
            tgt = _rdw(ram, (base + r[reg]) & 0xFFFFFFFF, 4)
            if tgt not in inst["cases"]:
                return ("ERR", pc)      # table miss -> FAIL (mirror default)
            pc = tgt
        elif kind == "call":
            if inst["set_pr"]:
                ns["pr"] = inst["ret_pc"]
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            pc = inst["target"]
        elif kind == "call_runtime":
            # RAM/ROM-slot indirect call: the runtime target value is
            # ignored; sh2emu executes the seeded nullsub (clean return)
            # so both sides continue together at ret_pc with pr=pc+4.
            if inst["set_pr"]:
                ns["pr"] = inst["ret_pc"]
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            pc = inst["ret_pc"]
        elif kind == "runtime_dispatch":
            # runtime-indexed jmp/jsr: dispatches to r[reg] if that
            # target was lifted into CODE (in-span); else jsr falls
            # through to ret_pc and jmp returns (RET).  Mirrors
            # sh2emu, which runs the real target on the shared seeded
            # RAM (in-span targets match; out-of-span targets drift,
            # but sh2emu escaping is caught by StepLimitExceeded->skip).
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            reg = inst["reg"]
            _tg = r[reg] & 0xFFFFFFFF
            _tgt = CODE.get(_tg)
            if inst["is_call"]:
                ns["pr"] = inst["ret_pc"]
                pc = inst["ret_pc"] if _tgt is None else _tg
            else:
                if _tgt is None:
                    return ("RET", [x & 0xFFFFFFFF for x in r],
                            list(_WRITES), dict(ram), ns["pr"] & 0xFFFFFFFF)
                pc = _tg
        elif kind == "branch":
            t = ns["T"]
            taken = True
            if inst["cond"] == "T":
                taken = (t == 1)
            elif inst["cond"] == "notT":
                taken = (t == 0)
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            pc = inst["target"] if taken else (pc + 4 if slot_py is not None else pc + 2)
        elif kind == "ret":
            if inst.get("rte"):
                # rte pops PC+SR (8 bytes) off the stack before the
                # delay slot / jump; the mirror returns via pr like rts.
                ns["sp"] = (ns["sp"] + 8) & 0xFFFFFFFF
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            r[15] = ns["sp"] & 0xFFFFFFFF
            pc = ns["pr"] & 0xFFFFFFFF
        else:
            py = inst["py"]
            if py:
                exec(py, ns)
            pc = pc + 2

def run(cpu, ram, a, b, c_, d):
    ram = dict(ram)
    cpu.call(ENTRY, r4=a, r5=b, r6=c_, r7=d, ram=ram, regs={15: STACK_TOP},
             max_steps=MAXSTEPS)
    return cpu.r[0] & 0xFFFFFFFF, [x & 0xFFFFFFFF for x in cpu.r], dict(cpu.ram), cpu.pr & 0xFFFFFFFF

def main():
    rnd = random.Random(SEED)
    cpu = SH2(ROM_BYTES)
    skipped = 0
    for caso in range(N):
        ram = {}
        if RAM_MIN is not None:
            for a in range(RAM_MIN - 0x400, RAM_MAX + 0x401):
                ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        for a in range(STACK_BASE, STACK_BASE + 0x400):
            ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        for _sl, _tv in SLOT_SEEDS:
            _tb = _tv.to_bytes(4, "big")
            for _i in range(4):
                ram[_sl + _i] = _tb[_i]
        a = rnd.randint(0, 0xFFFFFFFF); b = rnd.randint(0, 0xFFFFFFFF)
        c_ = rnd.randint(0, 0xFFFFFFFF); d = rnd.randint(0, 0xFFFFFFFF)
        m = spec_mirror(a, b, c_, d, dict(ram))
        if m[0] != "RET":
            if m[0] == "SKIP":
                try:
                    run(cpu, ram, a, b, c_, d)
                except StepLimitExceeded:
                    continue
                except (NotImplementedError, RuntimeError):
                    skipped += 1; continue
                print("MISMATCH case=%d mirror=SKIP emu=RET" % (caso,))
                sys.exit(1)
            skipped += 1; continue
        try:
            g = run(cpu, ram, a, b, c_, d)
        except (StepLimitExceeded, NotImplementedError, RuntimeError):
            skipped += 1; continue
        _, exp_regs, _, exp_ram, exp_pr = m
        _, got_regs, got_ram, got_pr = g
        for i in range(16):
            if exp_regs[i] != got_regs[i]:
                print("MISMATCH case=%d reg=r%d mirror=%08X emu=%08X" % (caso, i, exp_regs[i], got_regs[i]))
                sys.exit(1)
        if exp_pr != got_pr:
            print("MISMATCH case=%d reg=pr mirror=%08X emu=%08X" % (caso, exp_pr, got_pr))
            sys.exit(1)
        for ad in sorted(set(exp_ram) | set(got_ram)):
            if exp_ram.get(ad, 0) != got_ram.get(ad, 0):
                print("MISMATCH case=%d addr=0x%08X mirror=%02X emu=%02X" % (caso, ad, exp_ram.get(ad, 0), got_ram.get(ad, 0)))
                sys.exit(1)
    ok = N - skipped
    has_ret = False
    seen = set(); stack = [ENTRY]
    while stack:
        pc = stack.pop()
        if pc in seen:
            continue
        seen.add(pc)
        inst = CODE.get(pc)
        if inst is None:
            continue
        k = inst["kind"]
        if k == "ret":
            has_ret = True; break
        if k == "branch":
            if inst["target"] is not None:
                stack.append(inst["target"])
            stack.append(pc + (4 if inst["slot_py"] is not None else 2))
        elif k == "call":
            stack.append(inst["ret_pc"])
        elif k == "call_runtime":
            stack.append(inst["ret_pc"])
        elif k == "runtime_dispatch":
            if inst["is_call"]:
                stack.append(inst["ret_pc"])
            stack.append(pc + (4 if inst["slot_py"] is not None else 2))
        elif k == "jt":
            stack.extend(c for c in inst["cases"] if c is not None)
            stack.append(pc + (4 if inst["slot_py"] is not None else 2))
        elif k == "dynbranch":
            stack.append(pc + (4 if inst["slot_py"] is not None else 2))
        else:
            stack.append(pc + 2)
    if ok == 0 and not has_ret:
        print("HALT (correct) %d/%d (skipped=%d)" % (ok, N, skipped))
        sys.exit(0)
    if skipped > 200 or ok == 0:
        print("FAIL %d/%d (skipped=%d)" % (ok, N, skipped))
        sys.exit(1)
    print("PASS %d/%d (skipped=%d)" % (ok, N, skipped))

if __name__ == "__main__":
    main()
