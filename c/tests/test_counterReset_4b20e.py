#!/usr/bin/env python3
"""Differential test for counterReset (0x4B20E) — v3 lift (branches + delay slots), 82 bytes.
Auto-generated by tools/gen_c_lift_v3.py — not human-verified.
Compares a Python pc-interpreter spec_mirror against the sh2emu oracle
(which runs the actual ROM bytes) over 500 random inputs: deterministic RAM
prefill around the literal addresses plus a synthetic 0x400-byte stack at
STACK_BASE.  Branch cond is sampled on T BEFORE the delay slot (as sh2emu);
cases where either side leaves the modeled span / exceeds max_steps are
skipped (StepLimitExceeded -> skip).
Run from repo root: python3 c/tests/test_counterReset_4b20e.py
"""
import os, random, sys

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, os.path.join(ROOT, "tools"))
from sh2emu import SH2, StepLimitExceeded
from c_lift_ops import s8, s16, s32

ROM = os.path.join(ROOT, "roms", "stock", "60E0FC00.bin")
ROM_BYTES = open(ROM, "rb").read()
ENTRY = 0x4B20E
RAW = bytes.fromhex("97 44 96 44 95 44 92 3E 63 20 23 38 8F 06 E4 00 25 40 26 40 27 40 93 3C A0 19 23 40 91 3A 60 10 20 08 8F 01 00 09 25 40 93 35 62 30 22 28 8F 01 00 09 26 40 92 30 63 20 23 38 8F 01 00 09 27 40 92 2B 63 20 23 38 8F 02 00 09 90 22 20 40 00 0B 00 09")
SEED = 42
N = 500
MAXSTEPS = 100000
STACK_BASE = 0xFFFFD000
STACK_TOP = STACK_BASE + 0x400
STACK_OFFS = ()
RAM_MIN = 0xFFFFCD58
RAM_MAX = 0xFFFFCD60
SPAN_END = 0x4B260
SENT = 0x4B260     # stop-sentinel, placed past the literal pool

_WRITES = []

def _rd(ram, a):
    a &= 0xFFFFFFFF
    v = ram.get(a)
    if v is not None:
        return v
    return ROM_BYTES[a] if a < len(ROM_BYTES) else 0

def _rdw(ram, a, n):
    a &= 0xFFFFFFFF
    return int.from_bytes(bytes(_rd(ram, (a + i) & 0xFFFFFFFF) for i in range(n)), "big")

def _wrw(ram, a, n, v):
    a &= 0xFFFFFFFF
    for i in range(n):
        ad = (a + i) & 0xFFFFFFFF
        ram[ad] = (v >> (8 * (n - 1 - i))) & 0xFF
        _WRITES.append(ad)

CODE = {
    0x4b20e: {"kind": 'reg', "py": 'r[7] = 0xFFFFCD59', "slot_py": None, "target": None, "cond": None},
    0x4b210: {"kind": 'reg', "py": 'r[6] = 0xFFFFCD58', "slot_py": None, "target": None, "cond": None},
    0x4b212: {"kind": 'reg', "py": 'r[5] = 0xFFFFCD5B', "slot_py": None, "target": None, "cond": None},
    0x4b214: {"kind": 'reg', "py": 'r[2] = 0xFFFFCD60', "slot_py": None, "target": None, "cond": None},
    0x4b216: {"kind": 'mem', "py": 'r[3] = s8(_rdw(ram, 0xFFFFCD60, 1))', "slot_py": None, "target": None, "cond": None},
    0x4b218: {"kind": 'reg', "py": 'T = 1 if (r[3] & r[3]) == 0 else 0', "slot_py": None, "target": None, "cond": None},
    0x4b21a: {"kind": "branch", "py": None, "slot_py": 'r[4] = s8(0x00)', "target": 0x4b22a, "cond": 'notT'},
    0x4b21e: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFCD5B, 1, r[4])', "slot_py": None, "target": None, "cond": None},
    0x4b220: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFCD58, 1, r[4])', "slot_py": None, "target": None, "cond": None},
    0x4b222: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFCD59, 1, r[4])', "slot_py": None, "target": None, "cond": None},
    0x4b224: {"kind": 'reg', "py": 'r[3] = 0xFFFFCD5A', "slot_py": None, "target": None, "cond": None},
    0x4b226: {"kind": "branch", "py": None, "slot_py": '_wrw(ram, 0xFFFFCD5A, 1, r[4])', "target": 0x4b25c, "cond": 'always'},
    0x4b22a: {"kind": 'reg', "py": 'r[1] = 0xFFFFCD5F', "slot_py": None, "target": None, "cond": None},
    0x4b22c: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFCD5F, 1))', "slot_py": None, "target": None, "cond": None},
    0x4b22e: {"kind": 'reg', "py": 'T = 1 if (r[0] & r[0]) == 0 else 0', "slot_py": None, "target": None, "cond": None},
    0x4b230: {"kind": "branch", "py": None, "slot_py": '', "target": 0x4b236, "cond": 'notT'},
    0x4b234: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFCD5B, 1, r[4])', "slot_py": None, "target": None, "cond": None},
    0x4b236: {"kind": 'reg', "py": 'r[3] = 0xFFFFCD5C', "slot_py": None, "target": None, "cond": None},
    0x4b238: {"kind": 'mem', "py": 'r[2] = s8(_rdw(ram, 0xFFFFCD5C, 1))', "slot_py": None, "target": None, "cond": None},
    0x4b23a: {"kind": 'reg', "py": 'T = 1 if (r[2] & r[2]) == 0 else 0', "slot_py": None, "target": None, "cond": None},
    0x4b23c: {"kind": "branch", "py": None, "slot_py": '', "target": 0x4b242, "cond": 'notT'},
    0x4b240: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFCD58, 1, r[4])', "slot_py": None, "target": None, "cond": None},
    0x4b242: {"kind": 'reg', "py": 'r[2] = 0xFFFFCD5D', "slot_py": None, "target": None, "cond": None},
    0x4b244: {"kind": 'mem', "py": 'r[3] = s8(_rdw(ram, 0xFFFFCD5D, 1))', "slot_py": None, "target": None, "cond": None},
    0x4b246: {"kind": 'reg', "py": 'T = 1 if (r[3] & r[3]) == 0 else 0', "slot_py": None, "target": None, "cond": None},
    0x4b248: {"kind": "branch", "py": None, "slot_py": '', "target": 0x4b24e, "cond": 'notT'},
    0x4b24c: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFCD59, 1, r[4])', "slot_py": None, "target": None, "cond": None},
    0x4b24e: {"kind": 'reg', "py": 'r[2] = 0xFFFFCD5E', "slot_py": None, "target": None, "cond": None},
    0x4b250: {"kind": 'mem', "py": 'r[3] = s8(_rdw(ram, 0xFFFFCD5E, 1))', "slot_py": None, "target": None, "cond": None},
    0x4b252: {"kind": 'reg', "py": 'T = 1 if (r[3] & r[3]) == 0 else 0', "slot_py": None, "target": None, "cond": None},
    0x4b254: {"kind": "branch", "py": None, "slot_py": '', "target": 0x4b25c, "cond": 'notT'},
    0x4b258: {"kind": 'reg', "py": 'r[0] = 0xFFFFCD5A', "slot_py": None, "target": None, "cond": None},
    0x4b25a: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFCD5A, 1, r[4])', "slot_py": None, "target": None, "cond": None},
    0x4b25c: {"kind": "ret", "py": None, "slot_py": '', "target": None, "cond": None},}

def spec_mirror(r4, r5, r6, r7, ram):
    """pc-interpreter over CODE; returns ("RET", regs, writes, ram, pr) or
    ("SKIP"/"ERR", detail).  Every instruction is the mapper py fragment
    exec'd in a shared ns (registers/T/ram/writes follow sh2emu)."""
    global _WRITES
    _WRITES[:] = []
    r = [0] * 16
    r[4], r[5], r[6], r[7] = r4 & 0xFFFFFFFF, r5 & 0xFFFFFFFF, r6 & 0xFFFFFFFF, r7 & 0xFFFFFFFF
    r[15] = STACK_TOP & 0xFFFFFFFF
    ns = {"r": r, "T": 0, "Q": 0, "M": 0, "mach": 0, "macl": 0, "pr": 0xEEEE0000,
          "sr": 0x000000F0,  # sh2emu call() default (independent of T)
          "s8": s8, "s16": s16, "s32": s32, "ram": ram, "sp": r[15],
          "local": {off: _rdw(ram, STACK_BASE + off, 4) for off in STACK_OFFS},
          "_rdw": _rdw, "_wrw": _wrw, "STACK_BASE": STACK_BASE}
    pc = ENTRY
    steps = 0
    while True:
        steps += 1
        if steps > MAXSTEPS:
            return ("SKIP", None)
        inst = CODE.get(pc)
        if inst is None:
            return ("ERR", pc)
        kind = inst["kind"]
        if kind == "branch":
            t = ns["T"]
            cond = inst["cond"]
            taken = True
            if cond == "T":
                taken = (t == 1)
            elif cond == "notT":
                taken = (t == 0)
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            if taken:
                pc = inst["target"]
            elif slot_py is not None:
                pc = pc + 4            # delayed: slot at P+2 already ran
            else:
                pc = pc + 2            # non-delayed bt/bf
        elif kind == "dynbranch":
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            if inst["set_pr"]:
                ns["pr"] = (pc + 4) & 0xFFFFFFFF
            pc = (pc + 4 + ns["r"][inst["reg"]]) & 0xFFFFFFFF
        elif kind == "ret":
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            r[15] = ns["sp"] & 0xFFFFFFFF
            return ("RET", [x & 0xFFFFFFFF for x in r], _WRITES, ram, ns["pr"] & 0xFFFFFFFF)
        else:
            py = inst["py"]
            if py:
                exec(py, ns)
            pc = pc + 2

def run(cpu, ram, a, b, c_, d):
    ram = dict(ram)
    ram[SENT] = 0x00; ram[SENT + 1] = 0x0B; ram[SENT + 2] = 0x00; ram[SENT + 3] = 0x09
    cpu.call(ENTRY, r4=a, r5=b, r6=c_, r7=d, ram=ram, regs={15: STACK_TOP},
             max_steps=MAXSTEPS)
    out = dict(cpu.ram)
    for i in range(4):
        out.pop(SENT + i, None)
    return cpu.r[0] & 0xFFFFFFFF, [x & 0xFFFFFFFF for x in cpu.r], out, cpu.pr & 0xFFFFFFFF

def main():
    rnd = random.Random(SEED)
    cpu = SH2(ROM_BYTES)
    skipped = 0
    for caso in range(N):
        ram = {}
        if RAM_MIN is not None:
            for a in range(RAM_MIN - 0x400, RAM_MAX + 0x401):
                ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        for a in range(STACK_BASE, STACK_BASE + 0x400):
            ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        a = rnd.randint(0, 0xFFFFFFFF)
        b = rnd.randint(0, 0xFFFFFFFF)
        c_ = rnd.randint(0, 0xFFFFFFFF)
        d = rnd.randint(0, 0xFFFFFFFF)
        m = spec_mirror(a, b, c_, d, dict(ram))
        if m[0] != "RET":
            skipped += 1
            continue
        try:
            g = run(cpu, ram, a, b, c_, d)
        except (StepLimitExceeded, NotImplementedError, RuntimeError):
            skipped += 1
            continue
        _, exp_regs, _, exp_ram, exp_pr = m
        _, got_regs, got_ram, got_pr = g
        for i in range(16):
            if exp_regs[i] != got_regs[i]:
                print("MISMATCH case=%d reg=r%d mirror=%08X emu=%08X" % (caso, i, exp_regs[i], got_regs[i]))
                sys.exit(1)
        if exp_pr != got_pr:
            print("MISMATCH case=%d reg=pr mirror=%08X emu=%08X" % (caso, exp_pr, got_pr))
            sys.exit(1)
        for ad in sorted(set(exp_ram) | set(got_ram)):
            if exp_ram.get(ad, 0) != got_ram.get(ad, 0):
                print("MISMATCH case=%d addr=0x%08X mirror=%02X emu=%02X" % (caso, ad, exp_ram.get(ad, 0), got_ram.get(ad, 0)))
                sys.exit(1)
    ok = N - skipped
    if skipped > 200 or ok == 0:
        print("FAIL %d/%d (skipped=%d)" % (ok, N, skipped))
        sys.exit(1)
    print("PASS %d/%d (skipped=%d)" % (ok, N, skipped))

if __name__ == "__main__":
    main()
