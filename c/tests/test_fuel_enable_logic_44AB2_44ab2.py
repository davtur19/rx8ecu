#!/usr/bin/env python3
"""Differential test for fuel_enable_logic_44AB2 (0x44AB2) — v3 lift (branches + delay slots), 66 bytes.
Auto-generated by tools/gen_c_lift_v3.py — not human-verified.
Compares a Python pc-interpreter spec_mirror against the sh2emu oracle
(which runs the actual ROM bytes) over 500 random inputs: deterministic RAM
prefill around the literal addresses plus a synthetic 0x400-byte stack at
STACK_BASE.  Branch cond is sampled on T BEFORE the delay slot (as sh2emu);
cases where either side leaves the modeled span / exceeds max_steps are
skipped (StepLimitExceeded -> skip).
Run from repo root: python3 c/tests/test_fuel_enable_logic_44AB2_44ab2.py
"""
import os, random, sys

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, os.path.join(ROOT, "tools"))
from sh2emu import SH2, StepLimitExceeded
from c_lift_ops import s8, s16, s32

ROM = os.path.join(ROOT, "roms", "stock", "60E1D400.bin")
ROM_BYTES = open(ROM, "rb").read()
ENTRY = 0x44AB2
RAW = bytes.fromhex("93 1F 95 1F 92 1F 60 20 60 0C 88 01 8F 0A 64 30 91 1A D2 0D 60 10 63 20 30 30 8F 03 00 09 E2 00 A0 0C 25 20 61 4C 21 18 8F 08 00 09 D3 08 60 30 60 0C 88 01 8F 02 00 09 E3 01 25 30 D2 04 00 0B 22 40")
SEED = 42
N = 500
MAXSTEPS = 100000
STACK_BASE = 0xFFFFD000
STACK_TOP = STACK_BASE + 0x400
STACK_OFFS = ()
RAM_MIN = 0xFFFFAADA
RAM_MAX = 0xFFFFCB0B
SPAN_END = 0x44AF4
SENT = 0x44B04     # stop-sentinel, placed past the literal pool

_WRITES = []

def _rd(ram, a):
    a &= 0xFFFFFFFF
    v = ram.get(a)
    if v is not None:
        return v
    return ROM_BYTES[a] if a < len(ROM_BYTES) else 0

def _rdw(ram, a, n):
    a &= 0xFFFFFFFF
    return int.from_bytes(bytes(_rd(ram, (a + i) & 0xFFFFFFFF) for i in range(n)), "big")

def _wrw(ram, a, n, v):
    a &= 0xFFFFFFFF
    for i in range(n):
        ad = (a + i) & 0xFFFFFFFF
        ram[ad] = (v >> (8 * (n - 1 - i))) & 0xFF
        _WRITES.append(ad)

CODE = {
    0x44ab2: {"kind": 'reg', "py": 'r[3] = 0xFFFFCAB6', "slot_py": None, "target": None, "cond": None},
    0x44ab4: {"kind": 'reg', "py": 'r[5] = 0xFFFFCABF', "slot_py": None, "target": None, "cond": None},
    0x44ab6: {"kind": 'reg', "py": 'r[2] = 0xFFFFAADA', "slot_py": None, "target": None, "cond": None},
    0x44ab8: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFAADA, 1))', "slot_py": None, "target": None, "cond": None},
    0x44aba: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x44abc: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x01) else 0', "slot_py": None, "target": None, "cond": None},
    0x44abe: {"kind": "branch", "py": None, "slot_py": 'r[4] = s8(_rdw(ram, 0xFFFFCAB6, 1))', "target": 0x44ad6, "cond": 'notT'},
    0x44ac2: {"kind": 'reg', "py": 'r[1] = 0xFFFFCAAF', "slot_py": None, "target": None, "cond": None},
    0x44ac4: {"kind": 'reg', "py": 'r[2] = 0x0007B3D9', "slot_py": None, "target": None, "cond": None},
    0x44ac6: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFCAAF, 1))', "slot_py": None, "target": None, "cond": None},
    0x44ac8: {"kind": 'mem', "py": 'r[3] = s8(_rdw(ram, 0x0007B3D9, 1))', "slot_py": None, "target": None, "cond": None},
    0x44aca: {"kind": 'reg', "py": 'T = 1 if r[0] == r[3] else 0', "slot_py": None, "target": None, "cond": None},
    0x44acc: {"kind": "branch", "py": None, "slot_py": '', "target": 0x44ad6, "cond": 'notT'},
    0x44ad0: {"kind": 'reg', "py": 'r[2] = s8(0x00)', "slot_py": None, "target": None, "cond": None},
    0x44ad2: {"kind": "branch", "py": None, "slot_py": '_wrw(ram, 0xFFFFCABF, 1, r[2])', "target": 0x44aee, "cond": 'always'},
    0x44ad6: {"kind": 'reg', "py": 'r[1] = r[4] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x44ad8: {"kind": 'reg', "py": 'T = 1 if (r[1] & r[1]) == 0 else 0', "slot_py": None, "target": None, "cond": None},
    0x44ada: {"kind": "branch", "py": None, "slot_py": '', "target": 0x44aee, "cond": 'notT'},
    0x44ade: {"kind": 'reg', "py": 'r[3] = 0xFFFFCB0B', "slot_py": None, "target": None, "cond": None},
    0x44ae0: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFCB0B, 1))', "slot_py": None, "target": None, "cond": None},
    0x44ae2: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x44ae4: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x01) else 0', "slot_py": None, "target": None, "cond": None},
    0x44ae6: {"kind": "branch", "py": None, "slot_py": '', "target": 0x44aee, "cond": 'notT'},
    0x44aea: {"kind": 'reg', "py": 'r[3] = s8(0x01)', "slot_py": None, "target": None, "cond": None},
    0x44aec: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFCABF, 1, r[3])', "slot_py": None, "target": None, "cond": None},
    0x44aee: {"kind": 'reg', "py": 'r[2] = 0xFFFFCB0B', "slot_py": None, "target": None, "cond": None},
    0x44af0: {"kind": "ret", "py": None, "slot_py": '_wrw(ram, 0xFFFFCB0B, 1, r[4])', "target": None, "cond": None},}

def spec_mirror(r4, r5, r6, r7, ram):
    """pc-interpreter over CODE; returns ("RET", regs, writes, ram, pr) or
    ("SKIP"/"ERR", detail).  Every instruction is the mapper py fragment
    exec'd in a shared ns (registers/T/ram/writes follow sh2emu)."""
    global _WRITES
    _WRITES[:] = []
    r = [0] * 16
    r[4], r[5], r[6], r[7] = r4 & 0xFFFFFFFF, r5 & 0xFFFFFFFF, r6 & 0xFFFFFFFF, r7 & 0xFFFFFFFF
    r[15] = STACK_TOP & 0xFFFFFFFF
    ns = {"r": r, "T": 0, "Q": 0, "M": 0, "mach": 0, "macl": 0, "pr": 0xEEEE0000,
          "s8": s8, "s16": s16, "s32": s32, "ram": ram, "sp": r[15],
          "local": {off: _rdw(ram, STACK_BASE + off, 4) for off in STACK_OFFS},
          "_rdw": _rdw, "_wrw": _wrw, "STACK_BASE": STACK_BASE}
    pc = ENTRY
    steps = 0
    while True:
        steps += 1
        if steps > MAXSTEPS:
            return ("SKIP", None)
        inst = CODE.get(pc)
        if inst is None:
            return ("ERR", pc)
        kind = inst["kind"]
        if kind == "branch":
            t = ns["T"]
            cond = inst["cond"]
            taken = True
            if cond == "T":
                taken = (t == 1)
            elif cond == "notT":
                taken = (t == 0)
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            if taken:
                pc = inst["target"]
            elif slot_py is not None:
                pc = pc + 4            # delayed: slot at P+2 already ran
            else:
                pc = pc + 2            # non-delayed bt/bf
        elif kind == "ret":
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            r[15] = ns["sp"] & 0xFFFFFFFF
            return ("RET", [x & 0xFFFFFFFF for x in r], _WRITES, ram, ns["pr"] & 0xFFFFFFFF)
        else:
            py = inst["py"]
            if py:
                exec(py, ns)
            pc = pc + 2

def run(cpu, ram, a, b, c_, d):
    ram = dict(ram)
    ram[SENT] = 0x00; ram[SENT + 1] = 0x0B; ram[SENT + 2] = 0x00; ram[SENT + 3] = 0x09
    cpu.call(ENTRY, r4=a, r5=b, r6=c_, r7=d, ram=ram, regs={15: STACK_TOP},
             max_steps=MAXSTEPS)
    out = dict(cpu.ram)
    for i in range(4):
        out.pop(SENT + i, None)
    return cpu.r[0] & 0xFFFFFFFF, [x & 0xFFFFFFFF for x in cpu.r], out, cpu.pr & 0xFFFFFFFF

def main():
    rnd = random.Random(SEED)
    cpu = SH2(ROM_BYTES)
    skipped = 0
    for caso in range(N):
        ram = {}
        if RAM_MIN is not None:
            for a in range(RAM_MIN - 0x400, RAM_MAX + 0x401):
                ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        for a in range(STACK_BASE, STACK_BASE + 0x400):
            ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        a = rnd.randint(0, 0xFFFFFFFF)
        b = rnd.randint(0, 0xFFFFFFFF)
        c_ = rnd.randint(0, 0xFFFFFFFF)
        d = rnd.randint(0, 0xFFFFFFFF)
        m = spec_mirror(a, b, c_, d, dict(ram))
        if m[0] != "RET":
            skipped += 1
            continue
        try:
            g = run(cpu, ram, a, b, c_, d)
        except (StepLimitExceeded, NotImplementedError, RuntimeError):
            skipped += 1
            continue
        _, exp_regs, _, exp_ram, exp_pr = m
        _, got_regs, got_ram, got_pr = g
        for i in range(16):
            if exp_regs[i] != got_regs[i]:
                print("MISMATCH case=%d reg=r%d mirror=%08X emu=%08X" % (caso, i, exp_regs[i], got_regs[i]))
                sys.exit(1)
        if exp_pr != got_pr:
            print("MISMATCH case=%d reg=pr mirror=%08X emu=%08X" % (caso, exp_pr, got_pr))
            sys.exit(1)
        for ad in sorted(set(exp_ram) | set(got_ram)):
            if exp_ram.get(ad, 0) != got_ram.get(ad, 0):
                print("MISMATCH case=%d addr=0x%08X mirror=%02X emu=%02X" % (caso, ad, exp_ram.get(ad, 0), got_ram.get(ad, 0)))
                sys.exit(1)
    ok = N - skipped
    if skipped > 200 or ok == 0:
        print("FAIL %d/%d (skipped=%d)" % (ok, N, skipped))
        sys.exit(1)
    print("PASS %d/%d (skipped=%d)" % (ok, N, skipped))

if __name__ == "__main__":
    main()
