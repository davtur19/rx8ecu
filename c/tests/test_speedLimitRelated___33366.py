#!/usr/bin/env python3
"""Differential test for speedLimitRelated__ (0x33366) — v3 lift + v4 FPU, 100 bytes.
Auto-generated by tools/gen_c_lift_v3.py — not human-verified.
Compares a Python pc-interpreter spec_mirror against the sh2emu oracle
(which runs the actual ROM bytes) over 2000 random inputs: deterministic RAM
prefill around the literal addresses plus a synthetic 0x400-byte stack at
STACK_BASE.  FR inputs are seeded per case as 16 uint32 bit patterns
fr_in[i] = (case*0x9E3779B1 + i*0x1000003) & 0xFFFFFFFF, filtered with
(x & 0x7F7FFFFF) | 0x3F800000 to keep every value a finite positive
float32 (sign cleared, exponent < 0xFF — no NaN/Inf/-0.0 in the diff).
The mirror converts bit patterns to float32 via bits2f (sh2emu
semantics); the oracle is fed the same patterns as float values via
cpu.call(..., fr={i: bits2f(fr_in[i]) ...}).  Compared: r0..r15, the 16
FR bit patterns (f2bits), fpul, pr and every written RAM address (T is
not part of the state call() exposes).  fsqrt of a negative input raises
ValueError on both sides -> counted as skip.  Branch cond is sampled on T
BEFORE the delay slot (as sh2emu); cases where either side leaves the
modeled span / exceeds max_steps are skipped.
Run from repo root: python3 c/tests/test_speedLimitRelated___33366.py
"""
import os, random, sys

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, os.path.join(ROOT, "tools"))
from sh2emu import SH2, StepLimitExceeded
from c_lift_ops import s8, s16, s32, ts, bits2f, f2bits

ROM = os.path.join(ROOT, "roms", "stock", "60E0FC00.bin")
ROM_BYTES = open(ROM, "rb").read()
ENTRY = 0x33366
RAW = bytes.fromhex("92 62 93 62 F4 28 65 30 91 60 94 60 66 10 61 5C 60 13 88 01 8F 03 00 09 D2 30 A0 1C F3 28 D0 2F F5 08 60 13 88 05 8D 04 00 09 60 13 88 06 8F 16 00 09 D2 2B 65 6C 63 20 63 3C 35 30 8F 03 00 09 D3 29 A0 07 00 09 D1 28 60 10 60 0C 35 00 8F 04 00 09 D3 26 F3 38 A0 03 F4 3A A0 01 F4 5A F4 5A F6 48 D3 23")
SEED = 0
N = 2000
MAXSTEPS = 100000
STACK_BASE = 0xFFFFD000
STACK_TOP = STACK_BASE + 0x400
STACK_OFFS = ()
RAM_MIN = 0xFFFFBFBC
RAM_MAX = 0xFFFFC007
SPAN_END = 0x333CA
SENT = 0x333CA     # stop-sentinel, placed past the literal pool

_WRITES = []

def _rd(ram, a):
    a &= 0xFFFFFFFF
    v = ram.get(a)
    if v is not None:
        return v
    return ROM_BYTES[a] if a < len(ROM_BYTES) else 0

def _rdw(ram, a, n):
    a &= 0xFFFFFFFF
    return int.from_bytes(bytes(_rd(ram, (a + i) & 0xFFFFFFFF) for i in range(n)), "big")

def _wrw(ram, a, n, v):
    a &= 0xFFFFFFFF
    for i in range(n):
        ad = (a + i) & 0xFFFFFFFF
        ram[ad] = (v >> (8 * (n - 1 - i))) & 0xFF
        _WRITES.append(ad)

CODE = {
    0x33366: {"kind": 'reg', "py": 'r[2] = 0xFFFFBFBC', "slot_py": None, "target": None, "cond": None},
    0x33368: {"kind": 'reg', "py": 'r[3] = 0xFFFFC007', "slot_py": None, "target": None, "cond": None},
    0x3336a: {"kind": 'fpu', "py": 'fr[4] = bits2f(_rdw(ram, 0xFFFFBFBC, 4))', "slot_py": None, "target": None, "cond": None},
    0x3336c: {"kind": 'mem', "py": 'r[5] = s8(_rdw(ram, 0xFFFFC007, 1))', "slot_py": None, "target": None, "cond": None},
    0x3336e: {"kind": 'reg', "py": 'r[1] = 0xFFFFBFF8', "slot_py": None, "target": None, "cond": None},
    0x33370: {"kind": 'reg', "py": 'r[4] = 0xFFFFC040', "slot_py": None, "target": None, "cond": None},
    0x33372: {"kind": 'mem', "py": 'r[6] = s8(_rdw(ram, 0xFFFFBFF8, 1))', "slot_py": None, "target": None, "cond": None},
    0x33374: {"kind": 'reg', "py": 'r[1] = r[5] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x33376: {"kind": 'reg', "py": 'r[0] = r[1]', "slot_py": None, "target": None, "cond": None},
    0x33378: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x01) else 0', "slot_py": None, "target": None, "cond": None},
    0x3337a: {"kind": "branch", "py": None, "slot_py": '', "target": 0x33384, "cond": 'notT'},
    0x3337e: {"kind": 'reg', "py": 'r[2] = 0x00078388', "slot_py": None, "target": None, "cond": None},
    0x33380: {"kind": "branch", "py": None, "slot_py": 'fr[3] = bits2f(_rdw(ram, 0x00078388, 4))', "target": 0x333bc, "cond": 'always'},
    0x33384: {"kind": 'reg', "py": 'r[0] = 0x00078394', "slot_py": None, "target": None, "cond": None},
    0x33386: {"kind": 'fpu', "py": 'fr[5] = bits2f(_rdw(ram, 0x00078394, 4))', "slot_py": None, "target": None, "cond": None},
    0x33388: {"kind": 'reg', "py": 'r[0] = r[1]', "slot_py": None, "target": None, "cond": None},
    0x3338a: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x05) else 0', "slot_py": None, "target": None, "cond": None},
    0x3338c: {"kind": "branch", "py": None, "slot_py": '', "target": 0x33398, "cond": 'T'},
    0x33390: {"kind": 'reg', "py": 'r[0] = r[1]', "slot_py": None, "target": None, "cond": None},
    0x33392: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x06) else 0', "slot_py": None, "target": None, "cond": None},
    0x33394: {"kind": "branch", "py": None, "slot_py": '', "target": 0x333c4, "cond": 'notT'},
    0x33398: {"kind": 'reg', "py": 'r[2] = 0x00078384', "slot_py": None, "target": None, "cond": None},
    0x3339a: {"kind": 'reg', "py": 'r[5] = r[6] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x3339c: {"kind": 'mem', "py": 'r[3] = s8(_rdw(ram, 0x00078384, 1))', "slot_py": None, "target": None, "cond": None},
    0x3339e: {"kind": 'reg', "py": 'r[3] = r[3] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x333a0: {"kind": 'reg', "py": 'T = 1 if r[5] == r[3] else 0', "slot_py": None, "target": None, "cond": None},
    0x333a2: {"kind": "branch", "py": None, "slot_py": '', "target": 0x333ac, "cond": 'notT'},
    0x333a6: {"kind": 'reg', "py": 'r[3] = 0x0007838C', "slot_py": None, "target": None, "cond": None},
    0x333a8: {"kind": "branch", "py": None, "slot_py": '', "target": 0x333ba, "cond": 'always'},
    0x333ac: {"kind": 'reg', "py": 'r[1] = 0x00078385', "slot_py": None, "target": None, "cond": None},
    0x333ae: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0x00078385, 1))', "slot_py": None, "target": None, "cond": None},
    0x333b0: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x333b2: {"kind": 'reg', "py": 'T = 1 if r[5] == r[0] else 0', "slot_py": None, "target": None, "cond": None},
    0x333b4: {"kind": "branch", "py": None, "slot_py": '', "target": 0x333c0, "cond": 'notT'},
    0x333b8: {"kind": 'reg', "py": 'r[3] = 0x00078390', "slot_py": None, "target": None, "cond": None},
    0x333ba: {"kind": 'fpu', "py": 'fr[3] = bits2f(_rdw(ram, 0x00078390, 4))', "slot_py": None, "target": None, "cond": None},
    0x333bc: {"kind": "branch", "py": None, "slot_py": '_wrw(ram, r[4], 4, f2bits(fr[3]))', "target": 0x333c6, "cond": 'always'},
    0x333c0: {"kind": "branch", "py": None, "slot_py": '_wrw(ram, r[4], 4, f2bits(fr[5]))', "target": 0x333c6, "cond": 'always'},
    0x333c4: {"kind": 'fpu', "py": '_wrw(ram, r[4], 4, f2bits(fr[5]))', "slot_py": None, "target": None, "cond": None},
    0x333c6: {"kind": 'fpu', "py": 'fr[6] = bits2f(_rdw(ram, r[4], 4))', "slot_py": None, "target": None, "cond": None},
    0x333c8: {"kind": 'reg', "py": 'r[3] = 0x00078398', "slot_py": None, "target": None, "cond": None},}

def spec_mirror(r4, r5, r6, r7, ram, fr_in, gbr=0):
    """pc-interpreter over CODE; returns ("RET", regs, writes, ram, pr,
    fr_bits, fpul) or ("SKIP"/"ERR", detail).  fr_in is 16 uint32 bit
    patterns; fr is the float32 mirror (bits2f) the FPU fragments run
    on — sh2emu semantics, exact same list the emulator builds from
    ts(v) in call()."""
    global _WRITES
    _WRITES[:] = []
    r = [0] * 16
    r[4], r[5], r[6], r[7] = r4 & 0xFFFFFFFF, r5 & 0xFFFFFFFF, r6 & 0xFFFFFFFF, r7 & 0xFFFFFFFF
    r[15] = STACK_TOP & 0xFFFFFFFF
    fr = [bits2f(x) for x in fr_in]
    ns = {"r": r, "T": 0, "Q": 0, "M": 0, "mach": 0, "macl": 0, "pr": 0xEEEE0000,
          "sr": 0x000000F0,  # sh2emu call() default (independent of T)
          "fr": fr, "fpul": 0, "fpscr": 0,
          "s8": s8, "s16": s16, "s32": s32, "ts": ts, "bits2f": bits2f,
          "f2bits": f2bits, "ram": ram, "sp": r[15],
          "gbr": gbr & 0xFFFFFFFF,
          "local": {off: _rdw(ram, STACK_BASE + off, 4) for off in STACK_OFFS},
          "_rdw": _rdw, "_wrw": _wrw, "STACK_BASE": STACK_BASE}
    pc = ENTRY
    steps = 0
    while True:
        steps += 1
        if steps > MAXSTEPS:
            return ("SKIP", None)
        inst = CODE.get(pc)
        if inst is None:
            if pc >= SPAN_END:      # fallthrough-end: emu hits the stop-sentinel rts
                r[15] = ns["sp"] & 0xFFFFFFFF
                return ("RET", [x & 0xFFFFFFFF for x in r], _WRITES, ram,
                        ns["pr"] & 0xFFFFFFFF,
                        [f2bits(f) for f in fr], ns["fpul"] & 0xFFFFFFFF,
                        ns["macl"] & 0xFFFFFFFF, ns["mach"] & 0xFFFFFFFF)
            return ("ERR", pc)
        kind = inst["kind"]
        if kind == "branch":
            t = ns["T"]
            cond = inst["cond"]
            taken = True
            if cond == "T":
                taken = (t == 1)
            elif cond == "notT":
                taken = (t == 0)
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            if taken:
                pc = inst["target"]
            elif slot_py is not None:
                pc = pc + 4            # delayed: slot at P+2 already ran
            else:
                pc = pc + 2            # non-delayed bt/bf
        elif kind == "dynbranch":
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            if inst["set_pr"]:
                ns["pr"] = (pc + 4) & 0xFFFFFFFF
            pc = (pc + 4 + ns["r"][inst["reg"]]) & 0xFFFFFFFF
        elif kind == "ret":
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            r[15] = ns["sp"] & 0xFFFFFFFF
            return ("RET", [x & 0xFFFFFFFF for x in r], _WRITES, ram,
                    ns["pr"] & 0xFFFFFFFF,
                    [f2bits(f) for f in fr], ns["fpul"] & 0xFFFFFFFF,
                    ns["macl"] & 0xFFFFFFFF, ns["mach"] & 0xFFFFFFFF)
        else:
            py = inst["py"]
            if py:
                exec(py, ns)
            pc = pc + 2

def run(cpu, ram, a, b, c_, d, fr_in, gbr=0):
    ram = dict(ram)
    ram[SENT] = 0x00; ram[SENT + 1] = 0x0B; ram[SENT + 2] = 0x00; ram[SENT + 3] = 0x09
    cpu.call(ENTRY, r4=a, r5=b, r6=c_, r7=d, ram=ram,
             fr={i: bits2f(fr_in[i]) for i in range(16)},
             regs={15: STACK_TOP, "gbr": gbr}, max_steps=MAXSTEPS)
    out = dict(cpu.ram)
    for i in range(4):
        out.pop(SENT + i, None)
    return (cpu.r[0] & 0xFFFFFFFF, [x & 0xFFFFFFFF for x in cpu.r], out,
            cpu.pr & 0xFFFFFFFF,
            [f2bits(cpu.fr[i]) for i in range(16)], cpu.fpul & 0xFFFFFFFF,
            cpu.macl & 0xFFFFFFFF, cpu.mach & 0xFFFFFFFF)

def main():
    rnd = random.Random(SEED)
    cpu = SH2(ROM_BYTES)
    skipped = 0
    for caso in range(N):
        ram = {}
        if RAM_MIN is not None:
            for a in range(RAM_MIN - 0x400, RAM_MAX + 0x401):
                ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        for a in range(STACK_BASE, STACK_BASE + 0x400):
            ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        a = rnd.randint(0, 0xFFFFFFFF)
        b = rnd.randint(0, 0xFFFFFFFF)
        c_ = rnd.randint(0, 0xFFFFFFFF)
        d = rnd.randint(0, 0xFFFFFFFF)
        gbr = rnd.randint(0, 0xFFFFFFFF)
        fr_in = [((caso * 0x9E3779B1 + i * 0x1000003) & 0xFFFFFFFF) for i in range(16)]
        fr_in = [((x & 0x7F7FFFFF) | 0x3F800000) for x in fr_in]  # finite, positive, no NaN/Inf
        try:
            m = spec_mirror(a, b, c_, d, dict(ram), fr_in, gbr)
        except ValueError:
            skipped += 1          # fsqrt of a negative input (both sides)
            continue
        if m[0] != "RET":
            skipped += 1
            continue
        try:
            g = run(cpu, ram, a, b, c_, d, fr_in, gbr)
        except (StepLimitExceeded, NotImplementedError, RuntimeError, ValueError):
            skipped += 1
            continue
        _, exp_regs, _, exp_ram, exp_pr, exp_fr, exp_fpul, exp_macl, exp_mach = m
        _, got_regs, got_ram, got_pr, got_fr, got_fpul, got_macl, got_mach = g
        for i in range(16):
            if exp_regs[i] != got_regs[i]:
                print("MISMATCH case=%d reg=r%d mirror=%08X emu=%08X" % (caso, i, exp_regs[i], got_regs[i]))
                sys.exit(1)
        for i in range(16):
            if exp_fr[i] != got_fr[i]:
                print("MISMATCH case=%d fr%d mirror=%08X emu=%08X" % (caso, i, exp_fr[i], got_fr[i]))
                sys.exit(1)
        if exp_fpul != got_fpul:
            print("MISMATCH case=%d fpul mirror=%08X emu=%08X" % (caso, exp_fpul, got_fpul))
            sys.exit(1)
        if exp_pr != got_pr:
            print("MISMATCH case=%d reg=pr mirror=%08X emu=%08X" % (caso, exp_pr, got_pr))
            sys.exit(1)
        if exp_macl != got_macl:
            print("MISMATCH case=%d reg=macl mirror=%08X emu=%08X" % (caso, exp_macl, got_macl))
            sys.exit(1)
        if exp_mach != got_mach:
            print("MISMATCH case=%d reg=mach mirror=%08X emu=%08X" % (caso, exp_mach, got_mach))
            sys.exit(1)
        for ad in sorted(set(exp_ram) | set(got_ram)):
            if exp_ram.get(ad, 0) != got_ram.get(ad, 0):
                print("MISMATCH case=%d addr=0x%08X mirror=%02X emu=%02X" % (caso, ad, exp_ram.get(ad, 0), got_ram.get(ad, 0)))
                sys.exit(1)
    ok = N - skipped
    if skipped > 200 or ok == 0:
        print("FAIL %d/%d (skipped=%d)" % (ok, N, skipped))
        sys.exit(1)
    print("PASS %d/%d (skipped=%d)" % (ok, N, skipped))

if __name__ == "__main__":
    main()
