#!/usr/bin/env python3
"""Differential test for getKnockSensorADC (0xC3CE) — v3 lift + v4 FPU, 120 bytes.
Auto-generated by tools/gen_c_lift_v3.py — not human-verified.
Compares a Python pc-interpreter spec_mirror against the sh2emu oracle
(which runs the actual ROM bytes) over 500 random inputs: deterministic RAM
prefill around the literal addresses plus a synthetic 0x400-byte stack at
STACK_BASE.  FR inputs are seeded per case as 16 uint32 bit patterns
fr_in[i] = (case*0x9E3779B1 + i*0x1000003) & 0xFFFFFFFF, filtered with
(x & 0x7F7FFFFF) | 0x3F800000 to keep every value a finite positive
float32 (sign cleared, exponent < 0xFF — no NaN/Inf/-0.0 in the diff).
The mirror converts bit patterns to float32 via bits2f (sh2emu
semantics); the oracle is fed the same patterns as float values via
cpu.call(..., fr={i: bits2f(fr_in[i]) ...}).  Compared: r0..r15, the 16
FR bit patterns (f2bits), fpul, pr and every written RAM address (T is
not part of the state call() exposes).  fsqrt of a negative input raises
ValueError on both sides -> counted as skip.  Branch cond is sampled on T
BEFORE the delay slot (as sh2emu); cases where either side leaves the
modeled span / exceeds max_steps are skipped.
Run from repo root: python3 c/tests/test_getKnockSensorADC_c3ce.py
"""
import os, random, sys

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, os.path.join(ROOT, "tools"))
from sh2emu import SH2, StepLimitExceeded
from c_lift_ops import s8, s16, s32, ts, bits2f, f2bits

ROM = os.path.join(ROOT, "roms", "stock", "60E1D400.bin")
ROM_BYTES = open(ROM, "rb").read()
ENTRY = 0xC3CE
RAW = bytes.fromhex("2F A6 D2 31 63 21 D1 31 21 31 D3 31 60 31 D2 31 22 01 D1 31 F3 18 D3 31 F3 3A C7 31 F5 08 D2 31 F2 5A D7 31 F3 78 D3 31 F3 3A 91 4C D0 31 D2 31 20 10 D3 31 E0 00 22 00 F4 8D D2 31 66 03 DD 32 23 00 D1 2E 21 00 F2 4A E1 02 D3 2E E0 00 D5 2F F3 4A DA 2F DB 30 DC 30 76 01 FC 57 36 13 F3 78 FB 37 63 D4 25 30 FA 47 75 01 8F F5 70 04 6A F6 6B F6 6C F6 00 0B 6D F6")
SEED = 0
N = 500
MAXSTEPS = 100000
STACK_BASE = 0xFFFFD000
STACK_TOP = STACK_BASE + 0x400
STACK_OFFS = (0x3FC, 0x400, 0x404, 0x408)
RAM_MIN = 0xFFFFA324
RAM_MAX = 0xFFFFA389
SPAN_END = 0xC446
SENT = 0xC446     # stop-sentinel, placed past the literal pool

_WRITES = []

def _rd(ram, a):
    a &= 0xFFFFFFFF
    v = ram.get(a)
    if v is not None:
        return v
    return ROM_BYTES[a] if a < len(ROM_BYTES) else 0

def _rdw(ram, a, n):
    a &= 0xFFFFFFFF
    return int.from_bytes(bytes(_rd(ram, (a + i) & 0xFFFFFFFF) for i in range(n)), "big")

def _wrw(ram, a, n, v):
    a &= 0xFFFFFFFF
    for i in range(n):
        ad = (a + i) & 0xFFFFFFFF
        ram[ad] = (v >> (8 * (n - 1 - i))) & 0xFF
        _WRITES.append(ad)

CODE = {
    0xc3ce: {"kind": 'mem', "py": 'local[0x3FC] = r[10]\n_wrw(ram, STACK_BASE + 0x3FC, 4, r[10])\nsp = (sp - 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xc3d0: {"kind": 'reg', "py": 'r[2] = 0x0007A178', "slot_py": None, "target": None, "cond": None},
    0xc3d2: {"kind": 'mem', "py": 'r[3] = s16(_rdw(ram, 0x0007A178, 2))', "slot_py": None, "target": None, "cond": None},
    0xc3d4: {"kind": 'reg', "py": 'r[1] = 0xFFFFA37E', "slot_py": None, "target": None, "cond": None},
    0xc3d6: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFA37E, 2, r[3])', "slot_py": None, "target": None, "cond": None},
    0xc3d8: {"kind": 'reg', "py": 'r[3] = 0x0007A17A', "slot_py": None, "target": None, "cond": None},
    0xc3da: {"kind": 'mem', "py": 'r[0] = s16(_rdw(ram, 0x0007A17A, 2))', "slot_py": None, "target": None, "cond": None},
    0xc3dc: {"kind": 'reg', "py": 'r[2] = 0xFFFFA37C', "slot_py": None, "target": None, "cond": None},
    0xc3de: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFA37C, 2, r[0])', "slot_py": None, "target": None, "cond": None},
    0xc3e0: {"kind": 'reg', "py": 'r[1] = 0x0007A1A4', "slot_py": None, "target": None, "cond": None},
    0xc3e2: {"kind": 'fpu', "py": 'fr[3] = bits2f(_rdw(ram, 0x0007A1A4, 4))', "slot_py": None, "target": None, "cond": None},
    0xc3e4: {"kind": 'reg', "py": 'r[3] = 0xFFFFA328', "slot_py": None, "target": None, "cond": None},
    0xc3e6: {"kind": 'fpu', "py": '_wrw(ram, 0xFFFFA328, 4, f2bits(fr[3]))', "slot_py": None, "target": None, "cond": None},
    0xc3e8: {"kind": 'reg', "py": 'r[0] = 0x0000C4B0', "slot_py": None, "target": None, "cond": None},
    0xc3ea: {"kind": 'fpu', "py": 'fr[5] = bits2f(_rdw(ram, 0x0000C4B0, 4))', "slot_py": None, "target": None, "cond": None},
    0xc3ec: {"kind": 'reg', "py": 'r[2] = 0xFFFFA360', "slot_py": None, "target": None, "cond": None},
    0xc3ee: {"kind": 'fpu', "py": '_wrw(ram, 0xFFFFA360, 4, f2bits(fr[5]))', "slot_py": None, "target": None, "cond": None},
    0xc3f0: {"kind": 'reg', "py": 'r[7] = 0x0007A1D0', "slot_py": None, "target": None, "cond": None},
    0xc3f2: {"kind": 'fpu', "py": 'fr[3] = bits2f(_rdw(ram, r[7], 4))', "slot_py": None, "target": None, "cond": None},
    0xc3f4: {"kind": 'reg', "py": 'r[3] = 0xFFFFA364', "slot_py": None, "target": None, "cond": None},
    0xc3f6: {"kind": 'fpu', "py": '_wrw(ram, 0xFFFFA364, 4, f2bits(fr[3]))', "slot_py": None, "target": None, "cond": None},
    0xc3f8: {"kind": 'reg', "py": 'r[1] = 0x000000FF', "slot_py": None, "target": None, "cond": None},
    0xc3fa: {"kind": 'reg', "py": 'r[0] = 0xFFFFA384', "slot_py": None, "target": None, "cond": None},
    0xc3fc: {"kind": 'reg', "py": 'r[2] = 0xFFFFA385', "slot_py": None, "target": None, "cond": None},
    0xc3fe: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFA384, 1, r[1])', "slot_py": None, "target": None, "cond": None},
    0xc400: {"kind": 'reg', "py": 'r[3] = 0xFFFFA386', "slot_py": None, "target": None, "cond": None},
    0xc402: {"kind": 'reg', "py": 'r[0] = s8(0x00)', "slot_py": None, "target": None, "cond": None},
    0xc404: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFA385, 1, r[0])', "slot_py": None, "target": None, "cond": None},
    0xc406: {"kind": 'fpu', "py": 'fr[4] = 0.0', "slot_py": None, "target": None, "cond": None},
    0xc408: {"kind": 'reg', "py": 'r[2] = 0xFFFFA32C', "slot_py": None, "target": None, "cond": None},
    0xc40a: {"kind": 'reg', "py": 'r[6] = r[0]', "slot_py": None, "target": None, "cond": None},
    0xc40c: {"kind": 'reg', "py": 'r[13] = 0x0007A164', "slot_py": None, "target": None, "cond": None},
    0xc40e: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFA386, 1, r[0])', "slot_py": None, "target": None, "cond": None},
    0xc410: {"kind": 'reg', "py": 'r[1] = 0xFFFFA324', "slot_py": None, "target": None, "cond": None},
    0xc412: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFA324, 1, r[0])', "slot_py": None, "target": None, "cond": None},
    0xc414: {"kind": 'fpu', "py": '_wrw(ram, 0xFFFFA32C, 4, f2bits(fr[4]))', "slot_py": None, "target": None, "cond": None},
    0xc416: {"kind": 'reg', "py": 'r[1] = s8(0x02)', "slot_py": None, "target": None, "cond": None},
    0xc418: {"kind": 'reg', "py": 'r[3] = 0xFFFFA348', "slot_py": None, "target": None, "cond": None},
    0xc41a: {"kind": 'reg', "py": 'r[0] = s8(0x00)', "slot_py": None, "target": None, "cond": None},
    0xc41c: {"kind": 'reg', "py": 'r[5] = 0xFFFFA389', "slot_py": None, "target": None, "cond": None},
    0xc41e: {"kind": 'fpu', "py": '_wrw(ram, 0xFFFFA348, 4, f2bits(fr[4]))', "slot_py": None, "target": None, "cond": None},
    0xc420: {"kind": 'reg', "py": 'r[10] = 0xFFFFA334', "slot_py": None, "target": None, "cond": None},
    0xc422: {"kind": 'reg', "py": 'r[11] = 0xFFFFA368', "slot_py": None, "target": None, "cond": None},
    0xc424: {"kind": 'reg', "py": 'r[12] = 0xFFFFA350', "slot_py": None, "target": None, "cond": None},
    0xc426: {"kind": 'reg', "py": 'r[6] = (r[6] + s8(0x01)) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xc428: {"kind": 'fpu', "py": '_wrw(ram, r[0] + r[12], 4, f2bits(fr[5]))', "slot_py": None, "target": None, "cond": None},
    0xc42a: {"kind": 'reg', "py": 'T = 1 if s32(r[6]) >= s32(r[1]) else 0', "slot_py": None, "target": None, "cond": None},
    0xc42c: {"kind": 'fpu', "py": 'fr[3] = bits2f(_rdw(ram, r[7], 4))', "slot_py": None, "target": None, "cond": None},
    0xc42e: {"kind": 'fpu', "py": '_wrw(ram, r[0] + r[11], 4, f2bits(fr[3]))', "slot_py": None, "target": None, "cond": None},
    0xc430: {"kind": 'mem', "py": 'r[3] = s8(_rdw(ram, 0x0007A164, 1))\nr[13] = (r[13] + 1) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xc432: {"kind": 'mem', "py": '_wrw(ram, r[5], 1, r[3])', "slot_py": None, "target": None, "cond": None},
    0xc434: {"kind": 'fpu', "py": '_wrw(ram, r[0] + r[10], 4, f2bits(fr[4]))', "slot_py": None, "target": None, "cond": None},
    0xc436: {"kind": 'reg', "py": 'r[5] = (r[5] + s8(0x01)) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xc438: {"kind": "branch", "py": None, "slot_py": 'r[0] = (r[0] + s8(0x04)) & 0xFFFFFFFF', "target": 0xc426, "cond": 'notT'},
    0xc43c: {"kind": 'mem', "py": 'r[10] = _rdw(ram, STACK_BASE + 0x3FC, 4)\nsp = (sp + 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xc43e: {"kind": 'mem', "py": 'r[11] = _rdw(ram, STACK_BASE + 0x400, 4)\nsp = (sp + 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xc440: {"kind": 'mem', "py": 'r[12] = _rdw(ram, STACK_BASE + 0x404, 4)\nsp = (sp + 4) & 0xFFFFFFFF', "slot_py": None, "target": None, "cond": None},
    0xc442: {"kind": "ret", "py": None, "slot_py": 'r[13] = _rdw(ram, STACK_BASE + 0x408, 4)\nsp = (sp + 4) & 0xFFFFFFFF', "target": None, "cond": None},}

def spec_mirror(r4, r5, r6, r7, ram, fr_in):
    """pc-interpreter over CODE; returns ("RET", regs, writes, ram, pr,
    fr_bits, fpul) or ("SKIP"/"ERR", detail).  fr_in is 16 uint32 bit
    patterns; fr is the float32 mirror (bits2f) the FPU fragments run
    on — sh2emu semantics, exact same list the emulator builds from
    ts(v) in call()."""
    global _WRITES
    _WRITES[:] = []
    r = [0] * 16
    r[4], r[5], r[6], r[7] = r4 & 0xFFFFFFFF, r5 & 0xFFFFFFFF, r6 & 0xFFFFFFFF, r7 & 0xFFFFFFFF
    r[15] = STACK_TOP & 0xFFFFFFFF
    fr = [bits2f(x) for x in fr_in]
    ns = {"r": r, "T": 0, "Q": 0, "M": 0, "mach": 0, "macl": 0, "pr": 0xEEEE0000,
          "sr": 0x000000F0,  # sh2emu call() default (independent of T)
          "fr": fr, "fpul": 0, "fpscr": 0,
          "s8": s8, "s16": s16, "s32": s32, "ts": ts, "bits2f": bits2f,
          "f2bits": f2bits, "ram": ram, "sp": r[15],
          "local": {off: _rdw(ram, STACK_BASE + off, 4) for off in STACK_OFFS},
          "_rdw": _rdw, "_wrw": _wrw, "STACK_BASE": STACK_BASE}
    pc = ENTRY
    steps = 0
    while True:
        steps += 1
        if steps > MAXSTEPS:
            return ("SKIP", None)
        inst = CODE.get(pc)
        if inst is None:
            return ("ERR", pc)
        kind = inst["kind"]
        if kind == "branch":
            t = ns["T"]
            cond = inst["cond"]
            taken = True
            if cond == "T":
                taken = (t == 1)
            elif cond == "notT":
                taken = (t == 0)
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            if taken:
                pc = inst["target"]
            elif slot_py is not None:
                pc = pc + 4            # delayed: slot at P+2 already ran
            else:
                pc = pc + 2            # non-delayed bt/bf
        elif kind == "dynbranch":
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            if inst["set_pr"]:
                ns["pr"] = (pc + 4) & 0xFFFFFFFF
            pc = (pc + 4 + ns["r"][inst["reg"]]) & 0xFFFFFFFF
        elif kind == "ret":
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            r[15] = ns["sp"] & 0xFFFFFFFF
            return ("RET", [x & 0xFFFFFFFF for x in r], _WRITES, ram,
                    ns["pr"] & 0xFFFFFFFF,
                    [f2bits(f) for f in fr], ns["fpul"] & 0xFFFFFFFF)
        else:
            py = inst["py"]
            if py:
                exec(py, ns)
            pc = pc + 2

def run(cpu, ram, a, b, c_, d, fr_in):
    ram = dict(ram)
    ram[SENT] = 0x00; ram[SENT + 1] = 0x0B; ram[SENT + 2] = 0x00; ram[SENT + 3] = 0x09
    cpu.call(ENTRY, r4=a, r5=b, r6=c_, r7=d, ram=ram,
             fr={i: bits2f(fr_in[i]) for i in range(16)},
             regs={15: STACK_TOP}, max_steps=MAXSTEPS)
    out = dict(cpu.ram)
    for i in range(4):
        out.pop(SENT + i, None)
    return (cpu.r[0] & 0xFFFFFFFF, [x & 0xFFFFFFFF for x in cpu.r], out,
            cpu.pr & 0xFFFFFFFF,
            [f2bits(cpu.fr[i]) for i in range(16)], cpu.fpul & 0xFFFFFFFF)

def main():
    rnd = random.Random(SEED)
    cpu = SH2(ROM_BYTES)
    skipped = 0
    for caso in range(N):
        ram = {}
        if RAM_MIN is not None:
            for a in range(RAM_MIN - 0x400, RAM_MAX + 0x401):
                ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        for a in range(STACK_BASE, STACK_BASE + 0x400):
            ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        a = rnd.randint(0, 0xFFFFFFFF)
        b = rnd.randint(0, 0xFFFFFFFF)
        c_ = rnd.randint(0, 0xFFFFFFFF)
        d = rnd.randint(0, 0xFFFFFFFF)
        fr_in = [((caso * 0x9E3779B1 + i * 0x1000003) & 0xFFFFFFFF) for i in range(16)]
        fr_in = [((x & 0x7F7FFFFF) | 0x3F800000) for x in fr_in]  # finite, positive, no NaN/Inf
        try:
            m = spec_mirror(a, b, c_, d, dict(ram), fr_in)
        except ValueError:
            skipped += 1          # fsqrt of a negative input (both sides)
            continue
        if m[0] != "RET":
            skipped += 1
            continue
        try:
            g = run(cpu, ram, a, b, c_, d, fr_in)
        except (StepLimitExceeded, NotImplementedError, RuntimeError, ValueError):
            skipped += 1
            continue
        _, exp_regs, _, exp_ram, exp_pr, exp_fr, exp_fpul = m
        _, got_regs, got_ram, got_pr, got_fr, got_fpul = g
        for i in range(16):
            if exp_regs[i] != got_regs[i]:
                print("MISMATCH case=%d reg=r%d mirror=%08X emu=%08X" % (caso, i, exp_regs[i], got_regs[i]))
                sys.exit(1)
        for i in range(16):
            if exp_fr[i] != got_fr[i]:
                print("MISMATCH case=%d fr%d mirror=%08X emu=%08X" % (caso, i, exp_fr[i], got_fr[i]))
                sys.exit(1)
        if exp_fpul != got_fpul:
            print("MISMATCH case=%d fpul mirror=%08X emu=%08X" % (caso, exp_fpul, got_fpul))
            sys.exit(1)
        if exp_pr != got_pr:
            print("MISMATCH case=%d reg=pr mirror=%08X emu=%08X" % (caso, exp_pr, got_pr))
            sys.exit(1)
        for ad in sorted(set(exp_ram) | set(got_ram)):
            if exp_ram.get(ad, 0) != got_ram.get(ad, 0):
                print("MISMATCH case=%d addr=0x%08X mirror=%02X emu=%02X" % (caso, ad, exp_ram.get(ad, 0), got_ram.get(ad, 0)))
                sys.exit(1)
    ok = N - skipped
    if skipped > 200 or ok == 0:
        print("FAIL %d/%d (skipped=%d)" % (ok, N, skipped))
        sys.exit(1)
    print("PASS %d/%d (skipped=%d)" % (ok, N, skipped))

if __name__ == "__main__":
    main()
