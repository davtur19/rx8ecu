#!/usr/bin/env python3
"""Differential test for idle_speed_range_validator (0x19DDE) — v3 lift + v4 FPU, 130 bytes.
Auto-generated by tools/gen_c_lift_v3.py — not human-verified.
Compares a Python pc-interpreter spec_mirror against the sh2emu oracle
(which runs the actual ROM bytes) over 500 random inputs: deterministic RAM
prefill around the literal addresses plus a synthetic 0x400-byte stack at
STACK_BASE.  FR inputs are seeded per case as 16 uint32 bit patterns
fr_in[i] = (case*0x9E3779B1 + i*0x1000003) & 0xFFFFFFFF, filtered with
(x & 0x7F7FFFFF) | 0x3F800000 to keep every value a finite positive
float32 (sign cleared, exponent < 0xFF — no NaN/Inf/-0.0 in the diff).
The mirror converts bit patterns to float32 via bits2f (sh2emu
semantics); the oracle is fed the same patterns as float values via
cpu.call(..., fr={i: bits2f(fr_in[i]) ...}).  Compared: r0..r15, the 16
FR bit patterns (f2bits), fpul, pr and every written RAM address (T is
not part of the state call() exposes).  fsqrt of a negative input raises
ValueError on both sides -> counted as skip.  Branch cond is sampled on T
BEFORE the delay slot (as sh2emu); cases where either side leaves the
modeled span / exceeds max_steps are skipped.
Run from repo root: python3 c/tests/test_idle_speed_range_validator_19dde.py
"""
import os, random, sys

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, os.path.join(ROOT, "tools"))
from sh2emu import SH2, StepLimitExceeded
from c_lift_ops import s8, s16, s32, ts, bits2f, f2bits

ROM = os.path.join(ROOT, "roms", "stock", "60E1D400.bin")
ROM_BYTES = open(ROM, "rb").read()
ENTRY = 0x19DDE
RAW = bytes.fromhex("E5 01 93 3F F4 38 D2 25 F5 28 D1 25 F7 5C D6 25 F3 18 D3 25 F7 31 F6 68 F2 38 D7 24 F6 21 F5 45 8D 02 E4 00 A0 04 27 50 F7 45 8F 01 00 09 27 40 D7 20 F3 68 F3 45 8D 02 00 09 A0 04 27 50 F6 45 8F 01 00 09 27 40 96 1E D3 1B 60 30 60 0C 88 01 8F 06 00 09 D2 16 60 20 60 0C 88 01 8D 0C 00 09 D3 0D 60 30 60 0C 88 01 8F 08 00 09 D2 11 60 20 60 0C 88 01 8F 02 00 09 A0 01 26 50 26 40 00 0B 00 09")
SEED = 42
N = 500
MAXSTEPS = 100000
STACK_BASE = 0xFFFFD000
STACK_TOP = STACK_BASE + 0x400
STACK_OFFS = ()
RAM_MIN = 0xFFFFA9E8
RAM_MAX = 0xFFFFB5B8
SPAN_END = 0x19E60
SENT = 0x19E98     # stop-sentinel, placed past the literal pool

_WRITES = []

def _rd(ram, a):
    a &= 0xFFFFFFFF
    v = ram.get(a)
    if v is not None:
        return v
    return ROM_BYTES[a] if a < len(ROM_BYTES) else 0

def _rdw(ram, a, n):
    a &= 0xFFFFFFFF
    return int.from_bytes(bytes(_rd(ram, (a + i) & 0xFFFFFFFF) for i in range(n)), "big")

def _wrw(ram, a, n, v):
    a &= 0xFFFFFFFF
    for i in range(n):
        ad = (a + i) & 0xFFFFFFFF
        ram[ad] = (v >> (8 * (n - 1 - i))) & 0xFF
        _WRITES.append(ad)

CODE = {
    0x19dde: {"kind": 'reg', "py": 'r[5] = s8(0x01)', "slot_py": None, "target": None, "cond": None},
    0x19de0: {"kind": 'reg', "py": 'r[3] = 0xFFFFB5B8', "slot_py": None, "target": None, "cond": None},
    0x19de2: {"kind": 'fpu', "py": 'fr[4] = bits2f(_rdw(ram, 0xFFFFB5B8, 4))', "slot_py": None, "target": None, "cond": None},
    0x19de4: {"kind": 'reg', "py": 'r[2] = 0x0006F8B0', "slot_py": None, "target": None, "cond": None},
    0x19de6: {"kind": 'fpu', "py": 'fr[5] = bits2f(_rdw(ram, 0x0006F8B0, 4))', "slot_py": None, "target": None, "cond": None},
    0x19de8: {"kind": 'reg', "py": 'r[1] = 0x0006F8B4', "slot_py": None, "target": None, "cond": None},
    0x19dea: {"kind": 'fpu', "py": 'fr[7] = fr[5]', "slot_py": None, "target": None, "cond": None},
    0x19dec: {"kind": 'reg', "py": 'r[6] = 0x0006F8E4', "slot_py": None, "target": None, "cond": None},
    0x19dee: {"kind": 'fpu', "py": 'fr[3] = bits2f(_rdw(ram, 0x0006F8B4, 4))', "slot_py": None, "target": None, "cond": None},
    0x19df0: {"kind": 'reg', "py": 'r[3] = 0x0006F8E8', "slot_py": None, "target": None, "cond": None},
    0x19df2: {"kind": 'fpu', "py": 'fr[7] = ts(fr[7] - fr[3])', "slot_py": None, "target": None, "cond": None},
    0x19df4: {"kind": 'fpu', "py": 'fr[6] = bits2f(_rdw(ram, r[6], 4))', "slot_py": None, "target": None, "cond": None},
    0x19df6: {"kind": 'fpu', "py": 'fr[2] = bits2f(_rdw(ram, 0x0006F8E8, 4))', "slot_py": None, "target": None, "cond": None},
    0x19df8: {"kind": 'reg', "py": 'r[7] = 0xFFFFAA00', "slot_py": None, "target": None, "cond": None},
    0x19dfa: {"kind": 'fpu', "py": 'fr[6] = ts(fr[6] - fr[2])', "slot_py": None, "target": None, "cond": None},
    0x19dfc: {"kind": 'fpu', "py": 'T = 1 if fr[5] > fr[4] else 0', "slot_py": None, "target": None, "cond": None},
    0x19dfe: {"kind": "branch", "py": None, "slot_py": 'r[4] = s8(0x00)', "target": 0x19e06, "cond": 'T'},
    0x19e02: {"kind": "branch", "py": None, "slot_py": '_wrw(ram, 0xFFFFAA00, 1, r[5])', "target": 0x19e0e, "cond": 'always'},
    0x19e06: {"kind": 'fpu', "py": 'T = 1 if fr[7] > fr[4] else 0', "slot_py": None, "target": None, "cond": None},
    0x19e08: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19e0e, "cond": 'notT'},
    0x19e0c: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFAA00, 1, r[4])', "slot_py": None, "target": None, "cond": None},
    0x19e0e: {"kind": 'reg', "py": 'r[7] = 0xFFFFAA01', "slot_py": None, "target": None, "cond": None},
    0x19e10: {"kind": 'fpu', "py": 'fr[3] = bits2f(_rdw(ram, r[6], 4))', "slot_py": None, "target": None, "cond": None},
    0x19e12: {"kind": 'fpu', "py": 'T = 1 if fr[3] > fr[4] else 0', "slot_py": None, "target": None, "cond": None},
    0x19e14: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19e1c, "cond": 'T'},
    0x19e18: {"kind": "branch", "py": None, "slot_py": '_wrw(ram, 0xFFFFAA01, 1, r[5])', "target": 0x19e24, "cond": 'always'},
    0x19e1c: {"kind": 'fpu', "py": 'T = 1 if fr[6] > fr[4] else 0', "slot_py": None, "target": None, "cond": None},
    0x19e1e: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19e24, "cond": 'notT'},
    0x19e22: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFAA01, 1, r[4])', "slot_py": None, "target": None, "cond": None},
    0x19e24: {"kind": 'reg', "py": 'r[6] = 0xFFFFA9E8', "slot_py": None, "target": None, "cond": None},
    0x19e26: {"kind": 'reg', "py": 'r[3] = 0xFFFFA9EE', "slot_py": None, "target": None, "cond": None},
    0x19e28: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFA9EE, 1))', "slot_py": None, "target": None, "cond": None},
    0x19e2a: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x19e2c: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x01) else 0', "slot_py": None, "target": None, "cond": None},
    0x19e2e: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19e3e, "cond": 'notT'},
    0x19e32: {"kind": 'reg', "py": 'r[2] = 0xFFFFAA00', "slot_py": None, "target": None, "cond": None},
    0x19e34: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFAA00, 1))', "slot_py": None, "target": None, "cond": None},
    0x19e36: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x19e38: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x01) else 0', "slot_py": None, "target": None, "cond": None},
    0x19e3a: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19e56, "cond": 'T'},
    0x19e3e: {"kind": 'reg', "py": 'r[3] = 0xFFFFA9F0', "slot_py": None, "target": None, "cond": None},
    0x19e40: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFA9F0, 1))', "slot_py": None, "target": None, "cond": None},
    0x19e42: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x19e44: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x01) else 0', "slot_py": None, "target": None, "cond": None},
    0x19e46: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19e5a, "cond": 'notT'},
    0x19e4a: {"kind": 'reg', "py": 'r[2] = 0xFFFFAA01', "slot_py": None, "target": None, "cond": None},
    0x19e4c: {"kind": 'mem', "py": 'r[0] = s8(_rdw(ram, 0xFFFFAA01, 1))', "slot_py": None, "target": None, "cond": None},
    0x19e4e: {"kind": 'reg', "py": 'r[0] = r[0] & 0xFF', "slot_py": None, "target": None, "cond": None},
    0x19e50: {"kind": 'reg', "py": 'T = 1 if s32(r[0]) == s8(0x01) else 0', "slot_py": None, "target": None, "cond": None},
    0x19e52: {"kind": "branch", "py": None, "slot_py": '', "target": 0x19e5a, "cond": 'notT'},
    0x19e56: {"kind": "branch", "py": None, "slot_py": '_wrw(ram, 0xFFFFA9E8, 1, r[5])', "target": 0x19e5c, "cond": 'always'},
    0x19e5a: {"kind": 'mem', "py": '_wrw(ram, 0xFFFFA9E8, 1, r[4])', "slot_py": None, "target": None, "cond": None},
    0x19e5c: {"kind": "ret", "py": None, "slot_py": '', "target": None, "cond": None},}

def spec_mirror(r4, r5, r6, r7, ram, fr_in):
    """pc-interpreter over CODE; returns ("RET", regs, writes, ram, pr,
    fr_bits, fpul) or ("SKIP"/"ERR", detail).  fr_in is 16 uint32 bit
    patterns; fr is the float32 mirror (bits2f) the FPU fragments run
    on — sh2emu semantics, exact same list the emulator builds from
    ts(v) in call()."""
    global _WRITES
    _WRITES[:] = []
    r = [0] * 16
    r[4], r[5], r[6], r[7] = r4 & 0xFFFFFFFF, r5 & 0xFFFFFFFF, r6 & 0xFFFFFFFF, r7 & 0xFFFFFFFF
    r[15] = STACK_TOP & 0xFFFFFFFF
    fr = [bits2f(x) for x in fr_in]
    ns = {"r": r, "T": 0, "Q": 0, "M": 0, "mach": 0, "macl": 0, "pr": 0xEEEE0000,
          "fr": fr, "fpul": 0, "fpscr": 0,
          "s8": s8, "s16": s16, "s32": s32, "ts": ts, "bits2f": bits2f,
          "f2bits": f2bits, "ram": ram, "sp": r[15],
          "local": {off: _rdw(ram, STACK_BASE + off, 4) for off in STACK_OFFS},
          "_rdw": _rdw, "_wrw": _wrw, "STACK_BASE": STACK_BASE}
    pc = ENTRY
    steps = 0
    while True:
        steps += 1
        if steps > MAXSTEPS:
            return ("SKIP", None)
        inst = CODE.get(pc)
        if inst is None:
            return ("ERR", pc)
        kind = inst["kind"]
        if kind == "branch":
            t = ns["T"]
            cond = inst["cond"]
            taken = True
            if cond == "T":
                taken = (t == 1)
            elif cond == "notT":
                taken = (t == 0)
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            if taken:
                pc = inst["target"]
            elif slot_py is not None:
                pc = pc + 4            # delayed: slot at P+2 already ran
            else:
                pc = pc + 2            # non-delayed bt/bf
        elif kind == "ret":
            slot_py = inst["slot_py"]
            if slot_py:
                exec(slot_py, ns)
            r[15] = ns["sp"] & 0xFFFFFFFF
            return ("RET", [x & 0xFFFFFFFF for x in r], _WRITES, ram,
                    ns["pr"] & 0xFFFFFFFF,
                    [f2bits(f) for f in fr], ns["fpul"] & 0xFFFFFFFF)
        else:
            py = inst["py"]
            if py:
                exec(py, ns)
            pc = pc + 2

def run(cpu, ram, a, b, c_, d, fr_in):
    ram = dict(ram)
    ram[SENT] = 0x00; ram[SENT + 1] = 0x0B; ram[SENT + 2] = 0x00; ram[SENT + 3] = 0x09
    cpu.call(ENTRY, r4=a, r5=b, r6=c_, r7=d, ram=ram,
             fr={i: bits2f(fr_in[i]) for i in range(16)},
             regs={15: STACK_TOP}, max_steps=MAXSTEPS)
    out = dict(cpu.ram)
    for i in range(4):
        out.pop(SENT + i, None)
    return (cpu.r[0] & 0xFFFFFFFF, [x & 0xFFFFFFFF for x in cpu.r], out,
            cpu.pr & 0xFFFFFFFF,
            [f2bits(cpu.fr[i]) for i in range(16)], cpu.fpul & 0xFFFFFFFF)

def main():
    rnd = random.Random(SEED)
    cpu = SH2(ROM_BYTES)
    skipped = 0
    for caso in range(N):
        ram = {}
        if RAM_MIN is not None:
            for a in range(RAM_MIN - 0x400, RAM_MAX + 0x401):
                ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        for a in range(STACK_BASE, STACK_BASE + 0x400):
            ram[a] = (a * 0x9E3779B1 + caso * 0x10003) & 0xFF
        a = rnd.randint(0, 0xFFFFFFFF)
        b = rnd.randint(0, 0xFFFFFFFF)
        c_ = rnd.randint(0, 0xFFFFFFFF)
        d = rnd.randint(0, 0xFFFFFFFF)
        fr_in = [((caso * 0x9E3779B1 + i * 0x1000003) & 0xFFFFFFFF) for i in range(16)]
        fr_in = [((x & 0x7F7FFFFF) | 0x3F800000) for x in fr_in]  # finite, positive, no NaN/Inf
        try:
            m = spec_mirror(a, b, c_, d, dict(ram), fr_in)
        except ValueError:
            skipped += 1          # fsqrt of a negative input (both sides)
            continue
        if m[0] != "RET":
            skipped += 1
            continue
        try:
            g = run(cpu, ram, a, b, c_, d, fr_in)
        except (StepLimitExceeded, NotImplementedError, RuntimeError, ValueError):
            skipped += 1
            continue
        _, exp_regs, _, exp_ram, exp_pr, exp_fr, exp_fpul = m
        _, got_regs, got_ram, got_pr, got_fr, got_fpul = g
        for i in range(16):
            if exp_regs[i] != got_regs[i]:
                print("MISMATCH case=%d reg=r%d mirror=%08X emu=%08X" % (caso, i, exp_regs[i], got_regs[i]))
                sys.exit(1)
        for i in range(16):
            if exp_fr[i] != got_fr[i]:
                print("MISMATCH case=%d fr%d mirror=%08X emu=%08X" % (caso, i, exp_fr[i], got_fr[i]))
                sys.exit(1)
        if exp_fpul != got_fpul:
            print("MISMATCH case=%d fpul mirror=%08X emu=%08X" % (caso, exp_fpul, got_fpul))
            sys.exit(1)
        if exp_pr != got_pr:
            print("MISMATCH case=%d reg=pr mirror=%08X emu=%08X" % (caso, exp_pr, got_pr))
            sys.exit(1)
        for ad in sorted(set(exp_ram) | set(got_ram)):
            if exp_ram.get(ad, 0) != got_ram.get(ad, 0):
                print("MISMATCH case=%d addr=0x%08X mirror=%02X emu=%02X" % (caso, ad, exp_ram.get(ad, 0), got_ram.get(ad, 0)))
                sys.exit(1)
    ok = N - skipped
    if skipped > 200 or ok == 0:
        print("FAIL %d/%d (skipped=%d)" % (ok, N, skipped))
        sys.exit(1)
    print("PASS %d/%d (skipped=%d)" % (ok, N, skipped))

if __name__ == "__main__":
    main()
