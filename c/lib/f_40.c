/* ROM: 60E0FC00 | Address: 0x40 | Size: 0 | STATUS: PINNED
 * Retained (NOT an orphan): live callers in c/lib reference f_40
 * (f_7C.c, f_4E0.c); deleting it breaks their compilation.
 * The CFG lift failed (callee-cfg 64 ('unmapped', 70)) so the body stays
 * an empty compilable stub.  New stubs go to tmp/stubs/ (see
 * _write_stub_lib in tools/gen_c_lift_v8.py), never to c/lib/.
 * Originally auto-generated by tools/gen_c_lift_v8.py — coverage stub. */
#include <stdint.h>
typedef struct {
    uint32_t r[16];
    uint32_t pr, T, Q, M, macl, mach, sr, vbr, gbr, fpul, fpscr;
    uint32_t fr[16];   /* FPU bit patterns (IEEE-754) */
    uint32_t ram_base; /* bank base (0 for 60E1D400-style flat test) */
} ST;

void f_40(ST *s)
{
    /* stub: callee-cfg 64 ('unmapped', 70) */
    return;
}
